# تطبيق العربية للكريستال - خطة المشروع الكاملة

## 📋 نظرة عامة
- **اسم المشروع:** تطبيق العربية للكريستال - E-Commerce App
- **التقنية:** Flutter (Cross-Platform)
- **المنصات:** iOS & Android
- **النوع:** متجر إلكتروني لعرض وبيع منتجات الكريستال مع نظام عروض متكامل

## 🎯 الهدف
بناء تطبيق متجر إلكتروني احترافي يتيح للعملاء:
- تصفح مجموعات الكريستال المتنوعة
- مشاهدة تفاصيل المنتجات بجودة عالية
- الاستفادة من العروض والخصومات
- إتمام عمليات الشراء بسهولة وأمان
- تتبع الطلبات

---

## 🏗️ المرحلة الأولى: التحليل والتخطيط

### 1.1 تحليل المتطلبات الوظيفية

**المستخدم العادي (العميل):**
- التسجيل وتسجيل الدخول (بريد/جوال/جوجل/أبل)
- تصفح المنتجات حسب الفئات
- البحث المتقدم عن المنتجات
- عرض تفاصيل المنتج (صور 360°، مواصفات، سعر)
- إضافة للسلة والمفضلة
- تطبيق كوبونات الخصم
- إتمام الشراء (الدفع الإلكتروني/الدفع عند الاستلام)
- تتبع حالة الطلب
- تقييم المنتجات
- الإشعارات (عروض جديدة، حالة الطلب)
- الدعم الفني والمحادثة المباشرة

**لوحة التحكم (الإدارة):**
- إدارة المنتجات (إضافة/تعديل/حذف)
- إدارة الفئات والمجموعات
- إدارة العروض والخصومات
- إدارة الطلبات وتحديث الحالة
- إدارة المستخدمين
- تقارير المبيعات والإحصائيات
- إدارة الإشعارات
- إدارة المحتوى (بانرات، إعلانات)

### 1.2 هيكل قاعدة البيانات (Firebase / Supabase)

**Collections:**
- `users`: uid, name, email, phone, avatar, address[], favorites[], createdAt
- `products`: id, name, description, price, discountPrice, images[], categoryId, stock, rating, reviews[], specifications, isFeatured
- `categories`: id, name, icon, image, parentId, order
- `orders`: id, userId, items[], total, status, paymentMethod, shippingAddress, trackingNumber, createdAt
- `coupons`: code, discountType, discountValue, expiryDate, usageLimit, usedCount
- `banners`: id, image, title, link, isActive, order
- `reviews`: id, userId, productId, rating, comment, images[], createdAt

---

## 🎨 المرحلة الثانية: التصميم والواجهات

### 2.1 نظام التصميم (Design System)

**الألوان:**
- primary: `#1A237E` (أزرق داكن)
- secondary: `#C9A227` (ذهبي)
- accent: `#E8EAF6` (أزرق فاتح)
- background: `#FFFFFF`
- surface: `#F5F5F5`
- error: `#B00020`
- success: `#4CAF50`

**الخطوط:**
- Arabic: "Cairo" أو "Tajawal"
- English: "Poppins"

**الرسوم المتحركة:**
- Hero Animations للصور
- Shimmer Loading Effects
- Smooth Page Transitions
- Micro-interactions على الأزرار

### 2.2 الشاشات الرئيسية
1. Splash Screen
2. Onboarding (3 صفحات)
3. Login/Register
4. Home Screen (App Bar + بحث + إشعارات، Banner Carousel، فئات أفقية، منتجات مميزة، عروض خاصة، New Arrivals)
5. Product Details (صور + Zoom، سعر وتقييم، وصف، كمية، إضافة للسلة، تقييمات)
6. Categories Screen
7. Search Screen (فلاتر)
8. Cart Screen (كوبون)
9. Checkout Screen
10. Orders Screen
11. Profile Screen
12. Favorites Screen
13. Notifications Screen
14. Admin Dashboard

---

## 💻 المرحلة الثالثة: البنية التقنية

### 3.1 هيكل المشروع (Clean Architecture)
```
lib/
├── main.dart
├── app.dart
├── config/ (routes, theme, constants)
├── core/ (errors, usecases, utils, widgets)
├── features/ (auth, home, products, cart, orders, profile, admin)
│   └── كل feature: data/domain/presentation (bloc, pages, widgets)
├── shared/ (models, services, helpers)
└── injection.dart
```

### 3.2 المكتبات المطلوبة
flutter_bloc, get_it, go_router, dio, connectivity_plus, hive, hive_flutter, shared_preferences,
firebase_core, firebase_auth, cloud_firestore, firebase_storage, firebase_messaging,
cached_network_image, shimmer, carousel_slider, flutter_rating_bar, photo_view,
flutter_stripe, google_maps_flutter, geolocator, flutter_local_notifications,
image_picker, url_launcher, share_plus, intl

---

## 🛒 المرحلة الرابعة: الميزات الأساسية

### 4.1 نظام المصادقة
- تسجيل بالبريد وكلمة المرور
- تسجيل برقم الجوال (OTP)
- تسجيل بـ Google
- تسجيل بـ Apple (iOS)
- استعادة كلمة المرور
- التحقق من البريد
- تسجيل الخروج
- حذف الحساب

### 4.2 نظام المنتجات
- عرض Grid/List
- تصفية: فئة، سعر، تقييم، متاح
- ترتيب: الأحدث، الأعلى تقييماً، السعر
- صور متعددة + تكبير
- مشاركة المنتج
- المفضلة (محلياً + سحابياً)
- مقارنة المنتجات

### 4.3 نظام السلة والشراء
- إضافة/حذف/تعديل الكمية
- حفظ السلة محلياً
- كوبون مع التحقق
- حساب الإجمالي (منتجات + شحن - خصم)
- اختيار عنوان التوصيل
- اختيار طريقة الدفع
- تأكيد الطلب مع رقم الطلب

---

## 🎁 المرحلة الخامسة: نظام العروض والخصومات

### 5.1 أنواع العروض
1. عروض البانر (Banner Offers)
2. عروض نسبية (Percentage)
3. عروض ثابتة (Fixed Amount)
4. عروض الشراء المجمّع (Bundle)
5. عروض السلة (Cart Offers)
6. كوبونات الخصم (Coupon Codes)
7. عروض محدودة الوقت (Flash Sales) - عد تنازلي
8. عروض الأعضاء (Member Offers)

### 5.2 هيكل العرض في DB
```json
{
  "offerId": "offer_001",
  "title": "عروض الكريستال الإيطالي",
  "type": "percentage",
  "discountValue": 25,
  "applicableTo": { "type": "category", "ids": ["cat_italian_crystal"] },
  "conditions": { "minPurchase": 0, "maxDiscount": 200, "usageLimit": 100, "usedCount": 45 },
  "validity": { "startDate": "...", "endDate": "...", "isActive": true },
  "bannerImage": "...",
  "displayOrder": 1,
  "createdAt": "..."
}
```

### 5.3 واجهة عرض العروض
- الصفحة الرئيسية: شريط Marquee، Banner Carousel، عروض اليوم + Countdown، بطاقات عروض الفئات، الأكثر مبيعاً
- شاشة العروض: فلترة حسب النوع، قائمة العروض النشطة، تفاصيل كل عرض، زر "تسوق الآن"

---

## 💳 المرحلة السادسة: الدفع والشحن

### 6.1 طرق الدفع
1. الدفع الإلكتروني: Visa/Mastercard (Stripe)، Apple Pay، Google Pay، مدى (Mada)
2. الدفع عند الاستلام (COD)
3. التحويل البنكي

### 6.2 نظام الشحن
- حساب تكلفة الشحن حسب الوزن/المنطقة
- تتبع الطلب برقم التتبع
- إشعارات حالة الشحن
- دمج مع شركات الشحن (SMSA، Aramex)

---

## 📊 المرحلة السابعة: لوحة تحكم الإدارة

1. Dashboard Overview (مبيعات يوم/شهر/سنة، عدد الطلبات، مستخدمين جدد، الأكثر مبيعاً، رسم بياني)
2. إدارة المنتجات (قائمة + بحث، إضافة/تعديل/حذف، استيراد/تصدير Excel)
3. إدارة الطلبات (قائمة + فلترة، تفاصيل، تحديث حالة، طباعة فاتورة)
4. إدارة العروض (إنشاء، تفعيل/تعطيل، إحصائيات)
5. إدارة المستخدمين
6. الإشعارات (إرسال للجميع/فئة/مستخدم)

---

## 🔔 المرحلة الثامنة: الإشعارات
- ✅ Push Notifications (FCM + Cloud Function `sendPushNotification`): منتج جديد، عرض جديد، طلب جديد (لإدارة)، تحديث حالة الطلب، منتج عاد متوفر
- ❌ تذكير سلة مهجورة
- ✅ In-App Notifications (شاشة مرتبطة بـ Firestore) + Badge (عدّاد غير المقروء)
- ✅ معالجة الضغط على الإشعار للتنقل (شاشة/FCM/إشعار محلي)

---

## 🧪 المرحلة التاسعة: الاختبار والجودة
- Unit Tests (منطق الأعمال، Repositories، Use Cases)
- Widget Tests (واجهات، تفاعلات)
- Integration Tests (تدفق كامل، دفع، إشعارات)
- اختبار الأداء (Lazy Loading، Caching، تحسين Firestore، تصغير الحجم)

---

## 🚀 المرحلة العاشرة: النشر
- **Android:** Keystore، ProGuard، Google Play Console، صور ترويجية، وصف
- **iOS:** حساب Apple Developer، Certificates، App Store Connect، اختبار أجهزة حقيقية

---

## 📱 ميزات إضافية مقترحة
1. الوضع المظلم (Dark Mode)
2. دعم اللغتين (عربي/إنجليزي) - RTL/LTR
3. الواقع المعزز (AR) لعرض المنتجات في المنزل
4. نظام النقاط والولاء
5. دردشة مباشرة مع الدعم
6. مقارنة جانبية للمنتجات
7. قائمة الأمنيات مشتركة
8. تقييمات بالصور
9. استرداد النقود
10. تكامل مع وسائل التواصل الاجتماعي

---

## 💡 نصائح للتنفيذ
- ابدأ بـ MVP: ركز على الميزات الأساسية أولاً
- استخدم Firebase لتوفير الوقت في Backend
- صمم للـ RTL من البداية
- اختر الصور بعناية (الكريستال يحتاج إضاءة واحترافية)
- اختبر على أجهزة حقيقية
- حافظ على التوثيق (README واضح)
- استخدم Git مع فروع منظمة

---

# 📊 مراجعة الحالة (تاريخ المراجعة: 2026-08-11)

## الحالة: ✅ منفذ بالكامل | ⚠️ منفذ جزئياً | ❌ غير منفذ

> هذه مراجعة كاملة بديلة لمراجعة 2026-08-09. صحّحت عدة بنود كانت موسومة ❌ رغم أنها منفَّذة فعلياً (لوحة التحكم، Countdown، Marquee، فلترة العروض)، وأضافت قسم **الأمان** (كان غائباً تماماً)، وتوثّق إعادة تصميم لوحة التحكم وتحصين Cloud Functions/Firestore التي تمت في هذه الجولة.

## المرحلة الأولى: التحليل والتخطيط
| البند | الحالة | ملاحظات |
|---|---|---|
| تحليل متطلبات العميل | ✅ | موثقة في أعلى هذا الملف |
| تحليل متطلبات لوحة التحكم | ✅ | موثقة |
| هيكل DB - نماذج منتجات/فئات/طلبات | ✅ | نماذج في `lib/shared/models/` |
| هيكل DB - نماذج مستخدمين/مراجعات/بانرات | ✅ | `app_user.dart` + `review.dart` + `banner.dart` |

## المرحلة الثانية: التصميم والواجهات
| الشاشة | الحالة | ملاحظات |
|---|---|---|
| نظام الألوان (أزرق #1A237E + ذهبي #C9A227) | ✅ | `core/constants/app_colors.dart` |
| الخطوط (Cairo + Poppins) | ✅ | مجهزة في `pubspec.yaml` |
| Dark Mode | ✅ | `darkTheme` + `ThemeMode.system` |
| RTL/LTR (عربي/إنجليزي) | ✅ | `flutter_localizations` + `LocalizationService` |
| Splash Screen | ✅ | `splash_screen.dart` + توجيه تلقائي حسب حالة Onboarding |
| Onboarding (3 صفحات) | ✅ | `onboarding_screen.dart` + حفظ الحالة في SharedPreferences |
| Login/Register | ⚠️ | بريد/كلمة مرور ✅ عبر Firebase — أزرار جوجل/أبل/OTP **موجودة بالواجهة لكن فارغة** (`onPressed: () {}` في `login_screen.dart:221-241`) ولا توجد حزمة `google_sign_in`/`sign_in_with_apple` بالمشروع |
| Home Screen | ✅ | بانر + فئات + مميزة + وصل حديثاً |
| Product Details | ⚠️ | صور متعددة + مواصفات + كمية + إضافة للسلة ✅ — **التكبير (Zoom) غير منفَّذ رغم أن حزمة `photo_view` مثبّتة بلا استخدام** |
| Categories Screen | ✅ | موجود |
| Search Screen | ✅ | بحث + فلاتر متقدمة (فئة/سعر/تقييم) + ترتيب + تبديل Grid/List |
| Cart Screen | ✅ | مع كوبون (متحقَّق خادمياً الآن عند إتمام الطلب) |
| Checkout Screen | ✅ | إتمام شراء كامل — الإجمالي الآن **يُحسب ويُتحقَّق من الخادم** (Cloud Function `placeOrder`) وليس من رقم يرسله التطبيق |
| Orders Screen | ✅ | مزامنة سحابية لطلبات المستخدم + تفاصيل وتتبع حالة |
| Profile Screen | ✅ | موجود |
| Favorites Screen | ✅ | حفظ المفضلة محلياً + سحابياً عبر `AuthService` |
| Notifications Screen | ⚠️ | واجهة + سجل فعلي مرتبط بـ Firestore ✅ — تذكير السلة المهجورة غير موجود |
| Hero Animations | ✅ | بين بطاقات المنتجات وصفحة التفاصيل |
| Page Transitions | ✅ | تلاشي + انزلاق عبر `CustomTransitionPage` |
| Micro-interactions | ✅ | `PressableScale` على البطاقات والفئات |
| **Admin Dashboard** | ✅ | **موجودة بالكامل ومُعاد تصميمها احترافياً بهذه الجولة** (انظر المرحلة السابعة) — كانت موسومة ❌ خطأً بالمراجعة السابقة |

## المرحلة الثالثة: البنية التقنية
| البند | الحالة | ملاحظات |
|---|---|---|
| Clean Architecture (features/data/domain/presentation) | ✅ | مهيكلة بشكل صحيح |
| go_router للمسارات | ✅ | `config/routes.dart` |
| flutter_bloc + get_it | ✅ | `product_bloc.dart` + `injection.dart` |
| المكتبات المطلوبة | ⚠️ | ناقص: `google_maps_flutter`, `geolocator` (لا حاجة فعلية لهما حالياً بلا خرائط/تتبع GPS) |
| firebase_core/firebase_auth/firestore/messaging | ✅ | Auth + Firestore + FCM مفعّلة ومُختبَرة (محاكي حقيقي) |
| firebase_storage | ➡️ | مُستبدل بـ **Cloudinary برفع مُوقَّع (Signed Upload)** — كان رفعاً غير موقَّع (unsigned preset) مكشوفاً بالكود، أُصلح أمنياً |
| stripe (flutter_stripe) | ✅ | **مكتمل خادمياً الآن**: `createPaymentIntent` يقرأ المبلغ من `order.total` المحسوب بالخادم، و`confirmOrderPayment` يتحقق من Stripe مباشرة قبل تعليم الطلب مدفوعاً |
| Cloud Functions | ✅ | وسّعت من دالتين (`sendPushNotification`, `onNewOrder`/`onOrderUpdate`) إلى 7: + `placeOrder`, `cancelOrder`, `createPaymentIntent` (مُعاد صياغتها)، `confirmOrderPayment`, `getCloudinarySignature` |
| Hive للتخزين المحلي | ✅ | مستخدمة في `cart_service.dart` |
| مستودع Git | ❌ | **لا يوجد `.git` بالمجلد إطلاقاً** — كل هذا العمل غير محفوظ بنظام تحكم إصدارات؛ خطر فقدان فعلي |

## المرحلة الرابعة: الميزات الأساسية
| الميزة | الحالة | ملاحظات |
|---|---|---|
| تسجيل الدخول (بريد/جوال/جوجل/أبل) | ⚠️ | بريد/كلمة مرور ✅ — جوجل/أبل/OTP أزرار واجهة فقط بلا منطق ولا حزم مثبَّتة |
| استعادة كلمة المرور / التحقق من البريد | ✅ | استعادة كلمة المرور ✅ — **التحقق من البريد الآن بكود من 6 أرقام عبر Gmail** (`sendEmailVerificationCode`/`verifyEmailCode`)، وليس رابط Firebase الافتراضي؛ بوابة `go_router` تُحوّل أي مستخدم مسجَّل غير موثَّق إلى `/verify-email` تلقائياً — يتطلب ضبط `firebase functions:config:set gmail.user=... gmail.app_password=...` قبل النشر |
| عرض المنتجات Grid/List | ✅ | مُختبَر |
| تصفية وترتيب | ✅ | مُختبَر |
| صور متعددة + تكبير | ⚠️ | المعرض ✅ — التكبير ❌ (انظر أعلاه) |
| مشاركة المنتج | ✅ | `share_plus` |
| المفضلة محلياً + سحابياً | ✅ | مُختبَر |
| مقارنة المنتجات | ✅ | `compare_screen.dart` + `CompareService` مُختبَر |
| السلة (إضافة/حذف/تعديل كمية) | ✅ | مُختبَر |
| كوبونات الخصم | ✅ | **الاستهلاك الفعلي (usedCount/usedBy) صار حصراً عبر Cloud Function** بدل كتابة مباشرة من العميل — كان ثغرة استغلال |
| حساب الإجمالي (منتجات + شحن - خصم) | ✅ | يُعاد حسابه بالخادم من أسعار `products` الحقيقية عند الطلب، وليس فقط بالعميل للعرض |
| اختيار عنوان التوصيل / طريقة الدفع | ✅ | مُختبَر |
| تأكيد الطلب برقم الطلب | ✅ | مُختبَر |

## المرحلة الخامسة: نظام العروض
| البند | الحالة | ملاحظات |
|---|---|---|
| نموذج العرض (كل 8 أنواع) | ✅ | `offer.dart` |
| شاشة العروض | ✅ | **بيانات حقيقية من Firestore عبر `OfferRepository`**، وليست بطاقات تجريبية ثابتة كما وُصفت سابقاً |
| Banner Carousel في الرئيسية | ✅ | موجود |
| Countdown للفلاش سيل | ✅ | **موجود فعلياً** — `offers/presentation/widgets/flash_countdown.dart` (كانت موسومة ❌ خطأً) |
| شريط Marquee للعروض | ✅ | **موجود فعلياً** — `offers/presentation/widgets/offers_marquee.dart` (كانت موسومة ❌ خطأً) |
| عرض الأعضاء | ✅ | **مفعّل فعلياً** — `OfferEngine` يتحقق من `memberEligible` (تسجيل الدخول) لعروض `OfferType.member` |
| فلترة العروض حسب النوع | ✅ | **موجودة** — `_FilterChips` بشاشة العروض (الكل/فلاش/نسبة/ثابت/مجمّع/سلة/أعضاء) |

## المرحلة السادسة: الدفع والشحن
| البند | الحالة | ملاحظات |
|---|---|---|
| الدفع الإلكتروني (Stripe) | ✅ | **مكتمل**: PaymentIntent بمبلغ مُتحقَّق خادمياً + تأكيد دفع فعلي من Stripe قبل اعتماد الطلب |
| Apple Pay / Google Pay / Mada | ❌ | غير موجود (يحتاج تفعيل إضافي داخل Stripe + إعدادات منصّة) |
| الدفع عند الاستلام (COD) | ✅ | متاح |
| التحويل البنكي | ✅ | متاح |
| حساب الشحن (وزن/منطقة) | ⚠️ | ثابت فقط (مجاني فوق 500 ر.س، وإلا 25 ر.س) — لا يوجد حساب بالوزن أو المنطقة |
| تتبع الطلب برقم التتبع | ⚠️ | حقل يدوي يُدخله المدير (`trackingNumber`/`carrier`) ويُعرض للعميل — **لا يوجد تكامل فعلي مع API أي شركة شحن** |
| دمج شركات الشحن (SMSA/Aramex) | ❌ | غير موجود |
| استرداد النقود (Refund) | ⚠️ | حالة `OrderStatus.returned` موجودة بالنموذج فقط — لا يوجد أي منطق استرداد فعلي (لا نداء Stripe refund ولا تدفق إدارة) |

## المرحلة السابعة: لوحة تحكم الإدارة
| البند | الحالة | ملاحظات |
|---|---|---|
| Dashboard Overview + إحصائيات | ✅ | مبيعات اليوم/الشهر/السنة، رسم بياني 7 أيام، الأكثر مبيعاً — **أُعيد تصميمها بالكامل بهذه الجولة** (رأس ترحيبي، بطاقات بظل وحدود، بطاقات أكثر مبيعاً مرقّمة) |
| إدارة المنتجات | ✅ | قائمة + بحث + CSV استيراد/تصدير + إضافة/تعديل/حذف — نموذج المنتج أُعيد تنظيمه لبطاقات قسمية (صور/معلومات أساسية/سعر ومخزون/تفاصيل إضافية) |
| إدارة الطلبات + تحديث الحالة | ✅ | فلترة بالحالة + تفاصيل + فاتورة قابلة للطباعة + تحديث حالة/تتبع |
| إدارة العروض | ✅ | تفعيل/تعطيل + إضافة/تعديل/حذف، نموذج مُقسَّم لبطاقات |
| إدارة المستخدمين | ✅ | قائمة + إحصائيات (إجمالي/عملاء/مدراء) |
| إرسال الإشعارات | ✅ | إرسال فوري + سجل مُرسَل |
| **تصميم موحّد** | ✅ | نظام تصميم مشترك جديد (`AdminPageHeader`, `AdminEmptyState`, `StatusPill`, `AdminSectionCard`) يوحّد كل الشاشات بدل تكرار كل شاشة بأسلوبها |
| شريط جانبي/تنقّل | ✅ | شريط جانبي بشعار وهوية المدير (سطح المكتب) + شريط سفلي (جوال)، عنوان الشريط العلوي ديناميكي حسب القسم |

> ⚠️ ملاحظة تحقق: لُوحظ فيضان تخطيط حقيقي (Overflow) بالبطاقات الإحصائية عند التشغيل الفعلي على محاكي أندرويد، وأُصلح بالكود (تصغير الحشوة/الأيقونة + `FittedBox` للأرقام + تعديل أبعاد الشبكة). **لم يُعَد التحقق البصري من الإصلاح بعد** بسبب بطء شديد بمحاكي بيئة العمل الحالية (محاكاة برمجية بلا تسريع أجهزة) — يُنصح بتجربته على جهاز حقيقي أو محاكي بتسريع فعلي.

## المرحلة الثامنة: الإشعارات
| البند | الحالة | ملاحظات |
|---|---|---|
| Push Notifications (firebase_messaging) | ✅ | FCM + Cloud Function (منتج/عرض/طلب/حالة/مخزون) |
| In-App Notifications | ✅ | شاشة مرتبطة بـ Firestore + تنقّل عند الضغط |
| شارة الإشعارات | ✅ | Badge مع عدّاد غير المقروء |
| إشعارات حالة الطلب | ✅ | طلبات جديدة للمدير + تحديث حالة للعميل |
| تذكير السلة المهجورة | ❌ | غير موجود |
| رمز FCM بالسجلات | ✅ | **أُصلح أمنياً** — كان يُطبع كاملاً بـ`debugPrint`، صار مقصوصاً (أول 8 محارف فقط) |

## المرحلة التاسعة: الاختبار والجودة
| البند | الحالة | ملاحظات |
|---|---|---|
| Unit Tests | ✅ | منطق المنتجات، المصادقة (بما فيها اختبارات أمان المدير المُحدَّثة)، السلة، الكوبونات، المقارنة، المفضلة، الإجماليات |
| Widget Tests | ✅ | **151 اختباراً ناجحاً** (نمت من 90) |
| `flutter analyze` | ✅ | **صفر أخطاء** — فقط 5 ملاحظات أسلوبية (info) بعد كل التعديلات |
| Integration Tests | ❌ | لا يوجد مجلد `integration_test/` |
| اختبار الأداء | ❌ | غير موجود |
| تشغيل فعلي على جهاز/محاكي | ⚠️ | تم تشغيله فعلياً مرة واحدة بنجاح (لقطة شاشة حقيقية للوحة التحكم)، وكشف ذلك مشكلة Overflow حقيقية أُصلحت — التحقق البصري من الإصلاح معلّق لبطء المحاكي |

## المرحلة العاشرة: النشر
| البند | الحالة | ملاحظات |
|---|---|---|
| Keystore + توقيع | ❌ | **النسخة release توقَّع حالياً بمفاتيح debug** (`android/app/build.gradle.kts:31-35`, تعليق "TODO: Add your own signing config") — **لا يجوز نشرها هكذا على Google Play إطلاقاً** |
| ProGuard/R8 | ❌ | لا يوجد `minifyEnabled`/`proguard-rules.pro` |
| رفع على Google Play | ❌ | لم يبدأ |
| iOS / App Store | ❌ | لم يبدأ |
| الصور الترويجية والوصف | ❌ | لم يبدأ |
| **نشر قواعد Firestore/Storage والدوال الجديدة** | ❌ | **الأهم**: كل إصلاحات الأمان بهذه الجولة (`firestore.rules`, `storage.rules`, `functions/index.js`) **محلية فقط ولم تُنشر بعد** — يتطلب `firebase login` + `firebase deploy --only firestore:rules,storage,functions` (لا تتوفر بيئة العمل على Firebase CLI مسجّلاً به) |

## الميزات الإضافية (محدَّثة 2026-08-11)
| الميزة | الحالة | ملاحظات |
|---|---|---|
| الوضع المظلم | ✅ | |
| دعم اللغتين RTL/LTR | ⚠️ | البنية جاهزة (`LocalizationService` + ملفي `app_strings`) لكن أغلب الشاشات تستدعي `AppStrings.` العربي مباشرة بدل مرجع ديناميكي — التبديل الفعلي للإنجليزية غير مُختبَر بمعظم الشاشات |
| مقارنة جانبية للمنتجات | ✅ | شاشة + خدمة فعّالتان، حد أقصى 4 منتجات، مُختبَرة |
| الواقع المعزز AR | ❌ | تم تجاوزها عمداً — تحتاج نماذج 3D حقيقية وجهازاً بكاميرا فعلية لن تتوفر بهذه البيئة |
| **نظام النقاط والولاء** | ✅ | **مُنفَّذ بالكامل**: نقطة لكل 10 ر.س تُكسَب عند تسليم الطلب (`onOrderUpdate` بالخادم)، وتُستخدم كخصم (10 نقاط = 1 ر.س) عند الدفع، رصيد محمي بالكامل من الكتابة المباشرة من العميل (`firestore.rules`)، سجل حركات كامل، شاشة `PointsScreen` |
| **دردشة مباشرة** | ✅ | **مُنفَّذة بالكامل**: محادثة Firestore بين كل عميل والإدارة (`supportChats/{uid}/messages`)، شاشة عميل + قائمة/محادثة إدارة، إشعار فوري للطرف الآخر عند رسالة جديدة |
| **تقييمات بالصور** | ✅ | **مُنفَّذة بالكامل**: تقييم نجمي + تعليق + حتى 3 صور (رفع موقَّع عبر Cloudinary)، متوسط تقييم المنتج وعدده يُعاد حسابهما خادمياً تلقائياً (`onReviewWrite`) |
| **استرداد النقود** | ✅ | **مُنفَّذ بالكامل**: العميل يطلب الاسترداد من تفاصيل طلب مُسلَّم، الإدارة توافق/ترفض من لوحة مخصصة، الموافقة تُنفّذ استرداداً فعلياً عبر Stripe للطلبات المدفوعة إلكترونياً (`processRefundRequest`) |
| قائمة الأمنيات المشتركة | ✅ | **مُنفَّذة**: نشر نسخة عامة من المفضلة + رمز مشاركة نصي (لا يوجد نظام Deep Link حقيقي بعد لعدم وجود دومين مُتحقَّق منه) |
| تكامل وسائل التواصل الاجتماعي | ⚠️ | مشاركة منتج + مشاركة عامة للتطبيق عبر `share_plus` — لا تسجيل دخول اجتماعي (منفصل، انظر بند جوجل/أبل بالمرحلة الرابعة) |

> ⚠️ كل الميزات الخمس أعلاه (نقاط/دردشة/تقييمات/استرداد/Wishlist) تعتمد على دوال Cloud Functions ومجموعات Firestore جديدة — **غير منشورة على الإنتاج بعد**، نفس ملاحظة قسم الأمان أدناه. `flutter analyze` نظيف و153/154 اختباراً ناجحاً (الفاشل الوحيد اختبار توقيت متقطّع (`offer_model_test.dart`) غير مرتبط بهذه التعديلات، ينجح منفرداً).

---

# 🔒 الأمان — ملخص هذه الجولة

راجعت المشروع أمنياً بالكامل ووجدت 5 ثغرات حرجة تم إصلاحها جميعها بالكود (تفاصيل كاملة أعلى المحادثة):

1. **تصعيد صلاحيات كامل** — أي مستخدم كان يقدر يكتب `role: admin` بمستنده الخاص ويصير مديراً. أُصلح بقفل حقل `role` في `firestore.rules` (المالك يعدّل بياناته الشخصية فقط).
2. **كلمة مرور مدير مكتوبة بالكود المصدري** (`admin@123`) — حُذفت بالكامل مع كل منطق `AdminBootstrap`؛ صلاحية الإدارة تُمنح الآن يدوياً فقط من Firebase Console.
3. **تلاعب بالأسعار** — الإجمالي والمبلغ المرسل لـ Stripe كانا يُحسبان بالعميل بالكامل. أُصلح بإنشاء الطلب وحساب السعر خادمياً (`placeOrder` Cloud Function) واستخدام `order.total` المحسوب خادمياً عند إنشاء أي عملية دفع.
4. **كوبونات قابلة للاستغلال** — أي مستخدم مسجَّل كان يقدر يعدّل `usedCount`/`usedBy` لأي كوبون. أُصلح بقفل الكتابة للإدارة فقط، والاستهلاك يتم حصراً عبر Cloud Function.
5. **رفع Cloudinary غير موقَّع** — `cloudName`/`uploadPreset` كانا مكشوفين بالكود يسمحان برفع خارجي غير محدود. أُصلح برفع موقَّع (`getCloudinarySignature`) يتحقق من صلاحية الإدارة لصور المنتجات.

بالإضافة: تشديد `storage.rules` (كانت تسمح لأي مستخدم بالكتابة بأي مسار)، قفل إنشاء/تعديل/حذف الطلبات من العميل مباشرة (فقط عبر Cloud Functions أو الإدارة)، منح الإدارة صلاحية `list` على مجموعة `users` (كانت لوحة المستخدمين ستفشل فعلياً بدونها)، وإخفاء رمز FCM من السجلات.

**⚠️ الأهم الآن: كل هذه الإصلاحات ملفات محلية فقط — لم تُنشر على مشروع Firebase الفعلي بعد.** التطبيق بالإنتاج حالياً لا يزال يعمل بالقواعد والدوال القديمة غير المُصلَحة حتى يُشغَّل:
```bash
firebase deploy --only firestore:rules,storage,functions
```

---

# ✅ ملخص تنفيذي

**تم إنجازه فعلياً (تراكمي):** الهيكلة الكاملة + كل الواجهات + الثيم + RTL/Dark Mode + المرحلة الرابعة كاملة (مصادقة، منتجات، مفضلة، سلة، كوبونات، إتمام شراء) + نظام عروض فعلي بكل مكوناته (Countdown/Marquee/فلترة/أعضاء) + **لوحة تحكم إدارة كاملة ومُعاد تصميمها احترافياً** + **دفع Stripe مكتمل خادمياً** + **تحصين أمني شامل (5 ثغرات حرجة مُصلَحة)** + **151 اختباراً ناجحاً + تحليل ساكن نظيف**.

**الأولوية القصوى (بالترتيب):**
1. **نشر القواعد والدوال المُصلَحة على Firebase** — بدونها كل إصلاحات الأمان بلا أثر فعلي على الإنتاج
2. **تهيئة Git** (`git init` + أول commit) — لا يوجد تحكم إصدارات إطلاقاً حالياً، خطر فقدان عمل
3. إعداد Keystore حقيقي للنشر (النسخة الحالية توقَّع بمفاتيح debug، غير صالحة لـ Google Play)
4. التحقق البصري من إصلاح Overflow بلوحة التحكم على جهاز/محاكي حقيقي بتسريع أجهزة
5. تفعيل تسجيل جوجل/أبل الفعلي (الأزرار موجودة لكن فارغة)، والتحقق من البريد الإلكتروني
6. تكبير صور المنتجات (Zoom) — `photo_view` مثبَّتة وغير مستخدمة
7. Integration Tests + اختبار أداء
8. حساب شحن حقيقي بالوزن/المنطقة + تكامل شركة شحن، أو على الأقل توضيح أن التتبع الحالي يدوي بالكامل

**التطبيق يتصل فعلياً بـ Firebase (Auth + Firestore + Cloud Functions) وCloudinary** ويرسل/يستقبل بيانات حقيقية، مع بنية دفع وتسعير محصّنة خادمياً الآن بدل الاعتماد على العميل.

- ابدأ بـ MVP: ركز على الميزات الأساسية أولاً
- استخدم Firebase لتوفير الوقت في Backend
- صمم للـ RTL من البداية
- اختر الصور بعناية (الكريستال يحتاج إضاءة واحترافية)
- اختبر على أجهزة حقيقية
- حافظ على التوثيق (README واضح)
- استخدم Git مع فروع منظمة
