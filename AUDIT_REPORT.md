# تقرير المراجعة الشاملة — العربية للكريستال

**التاريخ:** 2026-08-17
**النطاق:** فحص أمني + أداء + جودة كود للمشروع كاملاً
**نتيجة التحقق:** `flutter analyze` → 0 أخطاء (5 ملاحظات تنسيق قديمة) · `flutter test` → 162/162 ناجح

---

## 1) المشاكل التي وُجدت (مصنّفة بالخطورة)

### 🔴 حرجة (Critical)

| # | المشكلة | الأثر |
|---|---|---|
| C1 | **قواعد Firestore كانت منشورة فعلياً كـ `allow read, write: if true`** | أي شخص على الإنترنت يقدر يقرأ ويكتب كل شيء: بيانات العملاء، الطلبات، الأسعار، صلاحيات الإدارة |
| C2 | **موافقة الاسترداد كانت معطّلة تماماً** — تنادي Cloud Function غير موجودة (`processRefundRequest`) | كل طلب استرداد يفشل بخطأ؛ الميزة ميتة في الإنتاج |
| C3 | **نقاط الولاء لم تُمنح ولا مرة واحدة** — لا يوجد أي كود يزيد رصيد النقاط | العميل يجمع "نقاط" وهمية؛ الميزة معروضة في الواجهة وغير عاملة |

### 🟠 عالية (High)

| # | المشكلة | الأثر |
|---|---|---|
| H1 | كود دفع إلكتروني ميت باقٍ في المشروع (`payment_service.dart`, `stripe_service.dart`, `stripe_config.dart` + حزمة `flutter_stripe`) | سطح هجوم بلا داعٍ + حجم تطبيق أكبر + التباس عند الصيانة |
| H2 | استعلامات بلا سقف: الإحصاءات تقرأ **كل** الطلبات والمستخدمين والمنتجات في كل فتحة للوحة، و`watchAllOrders` تبثّ المجموعة كاملة باستمرار | ينمو بلا حدود؛ عند 10 آلاف طلب = آلاف قراءات في الثانية + احتمال نفاد ذاكرة الجهاز + تجاوز حصة الخطة المجانية |
| H3 | `_matchingUserIds` (فئات الكوبون) تقرأ كل المستخدمين وكل الطلبات في كل حفظ كوبون | نفس المشكلة، ومحفّزها زر يضغطه المدير كثيراً |
| H4 | العميل المسجَّل كان يقدر تصفير مخزون أي منتج بكتابة واحدة | تخريب مخزون / إخفاء المنتجات عن البيع |

### 🟡 متوسطة (Medium)

| # | المشكلة | الحالة |
|---|---|---|
| M1 | **إشعار موجَّه لعميل واحد يقدر أي عميل آخر قراءته** | ⚠️ **لم تُصلَح بقرار مقصود** — التفاصيل في القسم 8 |
| M2 | `rating` و `reviewCount` للمنتج لا يُعاد حسابهما أبداً (الدالة الخادمية غير منشورة) | لم تُصلَح — تحتاج قراراً (القسم 8) |
| M3 | التحقق من البريد بالرمز غير عامل أصلاً (يعتمد Cloud Functions) والبوابة معطّلة الآن بالطلب | مقصود مؤقتاً |
| M4 | فهارس Firestore المركّبة كانت ناقصة → "حدث خطأ غير متوقع" | ✅ صُلحت (4 فهارس + `firestore.indexes.json`) |

### 🟢 منخفضة (Low)

- 5 ملاحظات تنسيق من المحلّل (`prefer_initializing_formals`, `use_null_aware_elements`) — تجميلية، لم تُغيَّر لتجنّب تعديلات لا قيمة أمنية لها.

---

## 2) الإصلاحات المُنفَّذة فعلياً

### الأمان أولاً

1. **`firestore.rules` أُعيدت كتابتها بالكامل** — لكل مجموعة قاعدة صريحة: منع تصعيد الصلاحيات (`role` لا يُعدَّل من العميل)، منع زيادة النقاط ذاتياً (`points` بالنقصان فقط)، الطلب يُنشأ بـ `userId` الحقيقي وبحالة `pending` فقط، التعديل للإدارة، أكواد التحقق ممنوعة كلياً على العميل، رسائل الدعم لا تُنتحل (`senderId == uid`).
2. **تقليص أثر تخريب المخزون** (H4) — الكتابة المسموحة للعميل الآن مقيّدة بـ: `stock` و `soldCount` فقط، لا تنقص أكثر من **20** في الكتابة الواحدة، **ويجب** أن تزيد `soldCount` بنفس مقدار نقص `stock` (فلا يمكن نقص بلا بيع).
3. **حذف كود الدفع الميت** (H1) + إزالة `flutter_stripe` من `pubspec.yaml`.
4. **قاعدة `refundRequests` update** مقصورة على المدير وعلى حقول القرار فقط (`status`, `resolvedAt`, `adminNote`) وبقيم `approved`/`rejected` حصراً — لا يقدر أحد تغيير صاحب الطلب أو مبلغه.
5. **`pointsLedger`**: قراءة لصاحبها/الإدارة، إنشاء للإدارة فقط، **تعديل وحذف ممنوعان نهائياً** (سجل غير قابل للتلاعب).

### الأداء ثانياً

6. `_statsLimit = 500` في `lib/features/admin/data/repositories/firestore_admin_repository.dart:40` — الطلبات تُقرأ مرتّبة تنازلياً بحدّ أعلى.
7. `watchAllOrders({int limit = 300})` في `lib/shared/services/order_service.dart:198`.
8. `_scanLimit = 1000` في `lib/shared/services/coupon_admin_service.dart:69`.

### استعادة ميزات مكسورة

9. **الاسترداد** (C2): `lib/shared/services/refund_service.dart` يكتب القرار مباشرة في Firestore بدل الدالة الخادمية المفقودة. آمن لأن الدفع عند الاستلام/تحويل بنكي — لا يوجد تحويل مالي آلي، الإرجاع يدوي والقرار يُسجَّل.
10. **نقاط الولاء** (C3): `awardPointsForDeliveredOrder` في `lib/shared/services/loyalty_service.dart` — معاملة ذرّية، **مقاومة للتكرار** (`pointsAwarded: true` على الطلب، فتعليم الطلب "تم التوصيل" مرتين لا يمنح النقاط مرتين)، موصولة بـ `_save()` في شاشة تفاصيل الطلب بلوحة الإدارة.

---

## 3) الأداء

- أثقل ثلاث نقاط في التطبيق كانت الاستعلامات غير المحدودة (H2/H3) — كلها مسقوفة الآن.
- الفهارس المركّبة الأربعة تحوّل استعلامات كانت **تفشل** إلى استعلامات مفهرسة.
- لم تُضَف أي `setState` أو إعادة بناء جديدة؛ البثّ اللحظي لتفاصيل الطلب يستخدم مستنداً واحداً (`watchOrder`) لا المجموعة.

## 4) قاعدة البيانات

- `firestore.indexes.json` منشور ومربوط في `firebase.json`.
- الكتابات الحسّاسة كلها داخل `runTransaction`: إنشاء الطلب (مخزون + نقاط + كوبون + الطلب ذرّياً)، ومنح النقاط.
- `pointsLedger` سجل إضافة-فقط (append-only) بقاعدة أمان.

## 5) التبعيات

- أُزيلت: `flutter_stripe`.
- لم تُرقَّ أي حزمة أخرى — ترقية الحزم في نهاية مراجعة أمنية تُدخل مخاطر انحدار بلا مقابل أمني واضح، والأفضل أن تكون خطوة منفصلة.

## 6) الاختبارات

`flutter test` → **162/162 ناجح** (كانت 156). المضاف:

- 3 اختبارات للمدى السعري في `test/features/product_filtering_test.dart`: المدى من–إلى، شمول الحدّين، وأن المدى يُطبَّق على السعر **المخفّض** لا الأصلي.
- 3 اختبارات للفئة المستهدفة في `test/services/coupon_service_test.dart`: الافتراضي = كل العملاء، صحة `toMap/fromMap` للفئة والحد، وأن فئة غير معروفة في البيانات تُعامَل كـ"كل العملاء" لا كخطأ (منع كسر التطبيق ببيانات مستقبلية).

---

## 7) مستوى الثقة

**لا يُقال إن الكود خالٍ من الثغرات.** التقدير:

| المجال | الثقة | السبب |
|---|---|---|
| صلاحيات Firestore | **عالية (~85%)** | كل مجموعة مراجَعة يدوياً، لكن قواعد Firestore لم تُختبر بمحاكي القواعد (`emulator`) — لا يوجد اختبار آلي يثبتها |
| منطق السلة/الكوبونات/الفلترة | **عالية (~90%)** | مغطّاة باختبارات آلية |
| سلامة أسعار الطلب | **متوسطة (~60%)** | القسم 8 — لا يوجد خادم موثوق |
| الأمان على مستوى الجهاز/الشبكة | **غير مفحوص** | لم يُفحص تخزين البيانات محلياً ولا تثبيت الشهادات |
| **التحقق على جهاز حقيقي** | **صفر** | المحاكي غير قابل للاستخدام على هذا العتاد — كل ما سبق مُتحقَّق منه بالتحليل الساكن والاختبارات فقط |

---

## 8) مشكلتان تحتاجان قراراً (لم تُنفَّذ)

### القرار الأول: سلامة الطلب والمخزون (الأهم)

لأن المشروع على خطة Firebase المجانية بلا خادم موثوق، **يجب** أن تسمح القواعد للعميل المسجَّل بالكتابة على `products.stock` و `coupons.usedCount` — وإلا فشل إتمام الطلب. النتيجة: عميل مسجَّل يقدر نظرياً (بأدوات، لا من التطبيق) تخفيض المخزون بلا شراء أو استنزاف عدد استخدامات كوبون. الأثر محدود (≤20 لكل كتابة + شرط تطابق المبيعات) لكنه غير مُغلَق.

الخيارات:

- **(أ) نقل إنشاء الطلب إلى خادم `alarabi-push` على Vercel** (الذي يعمل الآن) مع التحقق من Firebase ID token، ثم **منع** كتابة العميل على `stock`/`usedCount` كلياً. ← الحل الجذري، مجاني، لكنه يلمس مسار الدفع الذي استقرّ للتو ويحتاج إعادة اختبار كاملة.
- **(ب) البقاء على الوضع الحالي** بالحدود المفروضة، وتقبّل المخاطرة (منخفضة عملياً لمتجر بعملاء معروفين).

### القرار الثاني: خصوصية الإشعارات (M1)

شُدّدت القاعدة أولاً ثم **تُراجَع عنها** لأنها كانت ستكسر ميزتين: إرسال إشعار لعميل محدد من لوحة الإدارة، وعرض الإشعارات نفسه (Firestore يُقيّم القراءة لكل مستند، فاستعلام المجموعة كاملة يفشل إن خالف مستند واحد القاعدة). التزاماً بشرط "لا تكسر ميزة قائمة". الإصلاح الصحيح = تشديد القاعدة + تعديل العميل ليستخدم استعلامين مدمجين + فهرس `(targetId, createdAt)`.

الخيارات: **(أ)** تنفيذ الإصلاح الكامل (3 ملفات + فهرس)، أو **(ب)** تركه مؤقتاً — الأثر تسريب نص إشعار، لا بيانات حساب.

> ملاحظة مرتبطة (M2): تقييم المنتج وعدد المراجعات لن يتحدّثا أبداً حتى يُحسبا في مكان ما — يمكن حسابهما في نفس الخادم عند اختيار (أ) في القرار الأول.
