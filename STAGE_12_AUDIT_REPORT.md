# تقرير المرحلة 12 — Production Hardening & Final Security Audit

**التاريخ:** 2026-08-20
**النوع:** مراجعة مستقلة فقط — **لم يُعدَّل أي ملف، ولم تُنفَّذ أي عملية كتابة على Production.**
**طريقة الفحص:** قراءة كامل الكود (Firestore Rules، push-server، Flutter client)، فحص Git الكامل، واختبار خارجي حقيقي عبر REST مباشرة على Production (قراءة/محاولات كتابة مرفوضة فقط — لا كتابة ناجحة واحدة نُفِّذت).

---

## A) المشاكل المكتشفة — مرتبة حسب الخطورة

### 🔴 Critical

#### C1 — سر إشعارات Push ثابت داخل التطبيق المُوزَّع، يسمح بانتحال هوية المتجر

- **الملف:** `lib/core/constants/push_config.dart:14` (القيمة) + `push-server/api/notify.js:130-135` (نقطة التحقق الوحيدة)
- **السيناريو:** `PushConfig.sharedSecret` قيمة نصية ثابتة مُضمَّنة في كود Dart المُترجَم داخل ملف APK الموزَّع لكل عميل (عادي أو إداري). أي شخص يستخرجها عبر أدوات فك تحزيم قياسية (`strings` على `libapp.so`، أو محلِّلات AOT) يحصل على المفتاح الوحيد الذي يتحقق منه `/api/notify`.
- **الأثر:** بعد الاستخراج، يقدر المهاجم استدعاء `/api/notify` مباشرة من خارج التطبيق بالكامل، ويرسل:
  - إشعاراً لكل المشتركين في `store_updates` (بثّ عام) بأي نص يريده — تصيّد/محتوى مسيء باسم المتجر.
  - إشعاراً **موجَّهاً لأي `userId` محدد** (الخادم يقرأ رموز أجهزته من Firestore بثقة كاملة بمجرد تمرير المعرّف الصحيح، ولا يتحقق أن الطالب يملك صلاحية استهداف ذلك المستخدم).
- **هل يُستغَل من خارج التطبيق؟** نعم بالكامل — لا حاجة لأي حساب أو رمز Firebase، فقط السر المُستخرَج.
- **طريقة الإثبات:** فك تحزيم أي نسخة مُوزَّعة من APK (`apktool`/`jadx` أو `strings` على المكتبة الأصلية)، البحث عن نمط سلسلة hex بطول 50، ثم `curl -X POST .../api/notify -H "x-shared-secret: <القيمة>" -d '{"title":"...","userId":"<أي معرّف>"}'`. لم أنفّذ هذه الخطوة عملياً (تتطلب فك تحزيم APK فعلي، وهذا خارج نطاق "فحص فقط") لكن المسار مؤكَّد من الكود مباشرة.
- **الإصلاح المقترح:** استبدال السر الثابت بتحقق من رمز هوية Firebase (نفس نمط `order.js`/`review.js`) — الخادم يستخرج `uid` من التوكن الموقَّع بدل الثقة بسر مشترك، ويمنع الاستهداف الموجَّه إلا لصاحب التوكن نفسه (أو الإدارة فقط عبر تحقق دور إضافي على الخادم).
- **يحتاج تغيير Architecture؟** تغيير متوسط — يتطلب تعديل `notify.js` ليتحقق من التوكن بدل الترويسة الحالية، وتعديل نقاط الاستدعاء في `push_trigger_service.dart` لإرسال توكن بدل السر الثابت للحالات التي يبدؤها العميل (إن وُجدت)؛ الحالات التي يبدؤها الخادم نفسه (تغيّر حالة الطلب من `push-server` مستقبلاً) لا تتأثر.
- **قد يسبب Regression؟** نعم إن نُفِّذ بعجلة — أي مسار حالي يستدعي `notify()` بدون جلسة مستخدم حيّة (مثل إشعار عام عند نشر منتج من لوحة الإدارة) يحتاج التأكد أنه يمرّر توكن الإدارة بدل السر.

---

### 🟠 High

#### H1 — استنزاف حصة قراءة Firestore المجانية اليومية (DoS خارجي كامل بلا أي تسجيل دخول)

- **الملف:** قواعد `products`/`coupons`/`offers`/`reviews` في `firestore.rules` (`allow read: if true`) + غياب أي حماية إضافية (`firebase.json`, لا يوجد App Check)
- **السيناريو:** المشروع على خطة Firebase Spark المجانية، ذات حصة قراءات يومية محدودة صارمة. كل هذه المجموعات قابلة للقراءة العامة بلا أي مصادقة عبر REST مباشرة (تحققت الآن: `curl` بلا أي رأس Authorization ينجح في قراءة `products/p1`).
- **الأثر:** سكربت بسيط يستدعي القراءة العامة بشكل متكرر (حتى بضع مئات الطلبات في الدقيقة) يستنزف حصة اليوم كاملة، فيتوقف **كل** وصول لـ Firestore لبقية اليوم — لكل المستخدمين الحقيقيين، وللخادم الموثوق نفسه (`push-server` يعتمد على نفس المشروع).
- **هل يُستغَل من خارج التطبيق؟** نعم بالكامل، بلا أي حساب أو توكن.
- **طريقة الإثبات:** `for i in $(seq 1 500); do curl -s -o /dev/null "https://firestore.googleapis.com/v1/projects/alarabi-crystal/databases/(default)/documents/products"; done` (لم أنفّذها — تخريبية فعلياً، تستنزف حصتكم الحقيقية).
- **الإصلاح المقترح:** تفعيل **Firebase App Check** (يتحقق أن الطلب صادر فعلاً من نسخة تطبيق أصلية، لا سكربت خارجي) — الحل الأنسب لخطة Spark بلا تكلفة إضافية. بديل جزئي: مراقبة حصة الاستخدام يدوياً من الكونسول.
- **يحتاج تغيير Architecture؟** لا — إضافة SDK (`firebase_app_check`) + تفعيل من الكونسول، بلا تغيير في القواعد أو منطق العميل.
- **قد يسبب Regression؟** منخفض، لكن يتطلب اختباراً حقيقياً (App Check أحياناً يحجب طلبات مشروعة إن أُعِدَّ بشكل خاطئ — يُنصح بتفعيله بوضع "Monitor" أولاً قبل "Enforce").

#### H2 — لا حدّ لعدد المحاولات على أي نقطة نهاية في push-server

- **الملف:** `push-server/api/order.js`, `review.js`, `notify.js` بالكامل — لا يوجد أي middleware أو منطق تحديد معدّل
- **السيناريو:** حتى بعد التحقق من الهوية، لا يوجد سقف لعدد الطلبات لكل مستخدم/IP في الدقيقة/الساعة. عميل مسجَّل (حساب حقيقي) يقدر استدعاء `/api/order` أو `/api/review` بمعدل مرتفع جداً.
- **الأثر:**
  1. **إفراغ مخزون حقيقي بسرعة:** عميل خبيث يبني سكربتاً يُنشئ عشرات/مئات الطلبات الحقيقية (كمية 1، منتج رخيص) بمعرّفات `checkoutId` مختلفة في ثوانٍ — كل طلب ناجح **يخصم مخزوناً حقيقياً فعلياً** (الدفع عند الاستلام لا يتطلب دفعاً فعلياً وقت الطلب، فالتكلفة على المهاجم صفر).
  2. **تكلفة تشغيلية:** كل استدعاء لـ `/api/review` يُعيد مسح كامل مراجعات المنتج (حتى 1000 مستند) ويكتب على مستند المنتج — استدعاءات متكررة سريعة تُنتج قراءات/كتابات مكلفة بلا أي فائدة حقيقية.
- **هل يُستغَل من خارج التطبيق؟** يتطلب حساباً حقيقياً موثَّقاً (توكن صالح)، لكن إنشاء الحساب نفسه مجاني وبلا أي تحقق (`requireEmailVerification = false` حالياً)، فالحاجز عملياً منخفض جداً.
- **طريقة الإثبات:** تسجيل حساب تجريبي، ثم سكربت يستدعي `/api/order` بـ`checkoutId` عشوائي جديد في كل مرة لمنتج بمخزون منخفض ومتابعة انخفاض `stock` بسرعة غير طبيعية. لم أنفّذها (تُنشئ طلبات حقيقية فعلية).
- **الإصلاح المقترح:** تحديد معدّل بسيط لكل `uid` (مثلاً عبر مستند عدّاد في Firestore، أو خدمة خارجية مثل Upstash Redis المجانية المتوافقة مع Vercel Edge) — سقف معقول (مثال: 10 طلبات/دقيقة لكل مستخدم على `/api/order`، 5 لكل مستخدم على `/api/review`).
- **يحتاج تغيير Architecture؟** تغيير متوسط — يتطلب مكوّناً جديداً لتتبع المعدّل (حتى لو بسيطاً)، لا يمسّ منطق التسعير/المخزون الحالي.
- **قد يسبب Regression؟** منخفض إن نُفِّذ بحدّ سخي (لن يزعج مستخدماً حقيقياً يشتري بشكل طبيعي)، لكن يحتاج اختباراً على سيناريو "عميل يعيد محاولة الدفع عدة مرات بعد فشل الشبكة" حتى لا يُحظر خطأً.

#### H3 — لا سقف على عدد الطلبات لكل حساب (احتيال/إفراغ مخزون حتى بلا تكرار سريع)

- **الملف:** `push-server/api/order.js` بالكامل — لا يوجد تحقق من عدد طلبات المستخدم الحديثة أو قيمتها الإجمالية قبل السماح بطلب جديد
- **السيناريو:** حتى مع تحديد معدّل زمني (H2)، لا يوجد سقف يومي/أسبوعي على عدد الطلبات "قيد الانتظار" غير المدفوعة لكل حساب. حساب واحد يقدر تجميع عشرات الطلبات الحقيقية (كل واحد يحجز مخزوناً فعلياً) دون أي تدخل حتى تكتشفها الإدارة يدوياً لاحقاً.
- **الأثر:** إفراغ مخزون منتجات معينة عمداً (تخريب تجاري من منافس مثلاً)، أو تكديس طلبات وهمية تُثقل عمل الإدارة اليدوي في المراجعة والشحن.
- **هل يُستغَل من خارج التطبيق؟** يتطلب حساباً موثَّقاً (سهل الإنشاء حالياً).
- **طريقة الإثبات:** نفس أسلوب H2.
- **الإصلاح المقترح:** سقف على عدد الطلبات "pending" غير المُعالَجة لكل `uid` في نفس الوقت (مثال: لا يزيد عن 3 طلبات pending متزامنة لكل حساب)، يُتحقَّق منه داخل معاملة `order.js` نفسها بقراءة إضافية على `orders` بفلترة `userId + status=='pending'`.
- **يحتاج تغيير Architecture؟** لا — إضافة تحقق داخل `buildOrder`/`order.js` باستخدام استعلام موجود أصلاً (`userCouponStats` يقرأ طلبات المستخدم بالفعل، يمكن توسيعه).
- **قد يسبب Regression؟** منخفض، لكن يحتاج قراراً تجارياً بالسقف المناسب (لا تقني بحت).

#### H4 — لا سقف عملي يمنع Sybil/تعدد الحسابات لاستغلال كوبونات "العملاء الجدد"

- **الملف:** `firestore.rules:108-109` (إنشاء حساب) + `push-server/lib/pricing.js:119-125` (فحص "newCustomers") + `lib/config/routes.dart:121` (`requireEmailVerification = false`)
- **السيناريو:** إنشاء حساب Firebase Auth مجاني وفوري، بلا تحقق بريد إلكتروني فعلي حالياً (مُعطَّل عمداً بطلب سابق منك). كوبون "للعملاء الجدد فقط" أو أي مكافأة ترحيبية تُستغَل عبر إنشاء حسابات وهمية متعددة (بريد + مرور فقط، بلا أي تحقق حقيقي).
- **الأثر:** استنزاف قيمة الكوبونات الترويجية المخصصة لعملاء جدد فقط عبر حسابات متكررة لنفس الشخص.
- **هل يُستغَل من خارج التطبيق؟** يتطلب فقط استخدام Firebase Auth SDK/REST العام (لا يحتاج التطبيق نفسه).
- **طريقة الإثبات:** إنشاء عدة حسابات ببيانات بريد مختلفة (حتى لو أدرِس وهمية) واستخدام نفس الكوبون من كل واحد.
- **الإصلاح المقترح:** إعادة تفعيل `requireEmailVerification` عند استقرار مسار التحقق (مرتبط بالثغرة L3 أدناه — التحقق الحالي معطّل تقنياً وليس فقط بالبوابة)، أو ربط أهلية "عميل جديد" بمعيار أصعب انتحاله (رقم جوال موثَّق مثلاً).
- **يحتاج تغيير Architecture؟** يعتمد على الحل — تفعيل بوابة موجودة أصلاً (بسيط)، أو بناء تحقق بديل (متوسط).
- **قد يسبب Regression؟** نعم إن أُعيد تفعيل `requireEmailVerification` بلا إصلاح L3 أولاً (سيُعطَّل تسجيل الدخول فعلياً لأن الكود الخلفي للتحقق غير موجود).

---

### 🟡 Medium

#### M1 — لا تحقق من إبطال (Revocation) رمز الهوية على الخادم

- **الملف:** `push-server/lib/google-auth.js:130-183` (`verifyIdToken`)
- **السيناريو:** التحقق يفحص التوقيع والخوارزمية والجمهور والمُصدِر والصلاحية الزمنية فقط — لا يستدعي واجهة Firebase الإضافية للتحقق من الإبطال الفوري (تعطيل حساب من الإدارة، أو تغيير كلمة مرور يُفترض أن يُبطل الجلسات القديمة).
- **الأثر:** حساب عُطِّل للتو من الإدارة (لسوء سلوك مثلاً) يبقى قادراً على تنفيذ عمليات عبر `order.js`/`review.js` لمدة تصل لساعة كاملة (عمر التوكن الطبيعي) رغم التعطيل.
- **هل يُستغَل من خارج التطبيق؟** يتطلب توكناً صالحاً مُصدَراً قبل التعطيل — نافذة زمنية محدودة (≤ ساعة).
- **طريقة الإثبات:** تعطيل حساب اختباري من Firebase Console فوراً بعد تسجيل دخوله، ثم محاولة طلب بتوكنه القديم قبل انتهاء صلاحيته.
- **الإصلاح المقترح:** استدعاء إضافي لـ `https://identitytoolkit.googleapis.com/v1/accounts:lookup` بامتيازات حساب الخدمة، ومقارنة `validSince`/حالة التعطيل قبل قبول التوكن — أو تقليل مدة صلاحية التوكن من إعدادات المشروع.
- **يحتاج تغيير Architecture؟** لا — إضافة استدعاء شبكة واحد داخل `verifyIdToken` (يبطئ كل طلب قليلاً، يُفضَّل تخزينه مؤقتاً بضع دقائق).
- **قد يسبب Regression؟** منخفض.

#### M2 — Cloudinary Unsigned Upload Preset قابل للاستغلال من خارج التطبيق بالكامل

- **الملف:** `lib/shared/services/storage_service.dart:35-36` (موثَّق ذاتياً في الكود بالفعل)
- **السيناريو:** `cloudName`/`uploadPreset` مُضمَّنان في التطبيق، ورفع الصور لا يتطلب أي مصادقة على مستوى Cloudinary نفسه.
- **الأثر:** أي طرف خارجي يستخرج القيمتين يقدر رفع ملفات مباشرة لحساب Cloudinary (استهلاك حصة التخزين المجانية، أو استضافة محتوى غير مرغوب باسم حسابكم) — بلا أي تكلفة مالية مباشرة حالياً، لكن مخاطرة سمعة/حصة.
- **هل يُستغَل من خارج التطبيق؟** نعم بالكامل.
- **طريقة الإثبات:** `curl -F "upload_preset=alarabi_crystal" -F "file=@أي_ملف" https://api.cloudinary.com/v1_1/hmgxjl7g/image/upload` من أي جهاز، بلا أي علاقة بالتطبيق.
- **الإصلاح المقترح:** تقييد الـ preset من لوحة Cloudinary نفسها (حدود حجم/صيغة صارمة، تفعيل "Unsigned uploads: restrict by folder"، أو تفعيل Moderation التلقائي). الحل الجذري (يتطلب خادماً) هو الرفع الموقَّع عبر `push-server` بدل رفع مباشر من العميل.
- **يحتاج تغيير Architecture؟** الحل الجذري نعم (نقطة نهاية رفع جديدة على الخادم)؛ التخفيف السريع لا (إعدادات Cloudinary فقط).
- **قد يسبب Regression؟** منخفض إن اقتصر الإصلاح على قيود Cloudinary؛ متوسط إن نُقل الرفع للخادم (يمسّ كل شاشات رفع الصور).

#### M3 — كود Cloud Functions قديم (952 سطراً، 14 دالة، يتضمن تكامل Stripe فعلياً) غير منشور لكنه مربوط في `firebase.json`

- **الملف:** `functions/index.js` (كامل) + `firebase.json:8-10` (`functions.source`)
- **السيناريو:** هذا الكود من معمارية سابقة (قبل push-server) — يتضمن `placeOrder`, `cancelOrder`, `createPaymentIntent`/`confirmOrderPayment` (Stripe فعلي)، `processRefundRequest`, `onReviewWrite`, `sendEmailVerificationCode`/`verifyEmailCode`. غير قابل للنشر حالياً (خطة Spark)، لكنه يبقى "حياً" في المستودع ومربوطاً بـ `firebase.json`.
- **الأثر:** لو تمت ترقية المشروع لخطة Blaze مستقبلاً، ونُفِّذ `firebase deploy` كاملاً (بلا تحديد نطاق) بغفلة، **تُنشر كل هذه الدوال الـ14 فجأة** — بما فيها مسار دفع Stripe حقيقي (يتعارض مع قرار العمل الحالي: الدفع عند الاستلام/التحويل البنكي فقط)، ومنطق `placeOrder`/`onReviewWrite` مكرَّر ومتعارض مع `push-server` الحالي (نظامان مصدر حقيقة لنفس البيانات في آن واحد).
- **هل يُستغَل من خارج التطبيق؟** لا مباشرة (يتطلب فعل نشر من طرفكم)، لكنه مخاطرة تشغيلية حقيقية بغياب الوعي بها وقت الترقية.
- **طريقة الإثبات:** قراءة الملف مباشرة — تم، لا حاجة لاختبار تنفيذي.
- **الإصلاح المقترح:** توثيق واضح (تعليق تحذيري بارز أعلى الملف)، أو نقل المجلد خارج نطاق `firebase.json` مؤقتاً حتى القرار بترقية الخطة.
- **يحتاج تغيير Architecture؟** لا — تنظيم/توثيق فقط.
- **قد يسبب Regression؟** لا، ما لم يُحذف شيء يُستخدَم فعلياً (لم يُستخدَم أي منه حالياً).

#### M4 — قراءة إحصاءات أهلية الكوبون خارج المعاملة الذرّية (نافذة تزامن ضيقة)

- **الملف:** `push-server/api/order.js:39-52` (`userCouponStats`) — استعلام `db.query` بلا تمرير معرّف المعاملة
- **السيناريو:** خلافاً لقراءات المنتج/المستخدم/الكوبون نفسه (داخل `batchGet` بمعرّف المعاملة)، فحص "هل العميل جديد؟" أو "هل تجاوزت مشترياته الحد؟" يعتمد على استعلام منفصل غير متزامن ذرّياً مع باقي المعاملة.
- **الأثر:** نافذة تزامن نظرية ضيقة جداً — طلبان شبه متزامنان لنفس العميل الجديد، كلاهما يستخدم كوبون "عملاء جدد" بنجاح (بدل رفض الثاني). الأثر المالي محدود (خصم إضافي واحد كحد أقصى)، ولا يمس المخزون أو حد الاستخدام العام للكوبون (تلك محمية ذرّياً فعلاً، انظر القسم B).
- **هل يُستغَل من خارج التطبيق؟** يتطلب توقيتاً دقيقاً لطلبين متزامنين من نفس الحساب — صعب التنفيذ العملي لكن ممكن نظرياً.
- **طريقة الإثبات:** إرسال طلبين متزامنين (نفس اللحظة تقريباً) بنفس كوبون "عملاء جدد" من حساب جديد بـ`checkoutId` مختلفين، ومراقبة هل طُبِّق الخصم على كليهما.
- **الإصلاح المقترح:** قبول المخاطرة كحد أقصى (تأثيرها المالي محدود بطلب إضافي واحد نادر الحدوث)، أو نقل الاستعلام داخل المعاملة (يرفع تكلفة كل طلب قليلاً).
- **يحتاج تغيير Architecture؟** لا.
- **قد يسبب Regression؟** لا.

#### M5 — تسريب وجود حساب عبر رسالة "البريد مسجَّل مسبقاً"

- **الملف:** `lib/shared/services/auth_gateway.dart` (دالة `_mapAuthError`، حالتا `'email-already-in-use'` و `'user-not-found'`)
- **السيناريو:** شاشة إنشاء حساب تعرض بوضوح "هذا البريد مسجل مسبقاً" لبريد مُسجَّل فعلاً — سلوك Firebase الحالي (`createUserWithEmailAndPassword`) يُرجع هذا الكود بوضوح فلا لبس فيه. أما شاشتا الدخول واستعادة كلمة المرور فيحملان معالجة منفصلة لـ`'user-not-found'` أيضاً؛ سلوك Firebase الحالي لهاتين تحديداً بدأ يُوحِّد الرسالة لمنع هذا التسريب من جهة الخادم نفسه (لا الكود)، فالمخاطرة الفعلية الأكيدة الآن هي شاشة التسجيل فقط.
- **الأثر:** إمكانية حصر قائمة عملاء المتجر عبر تجربة بريد إلكتروني بريد إلكتروني (هجوم تعداد Enumeration) — مخاطرة خصوصية لا مالية مباشرة.
- **هل يُستغَل من خارج التطبيق؟** يتطلب استخدام واجهة التسجيل نفسها (أو REST المكافئ لها) بلا حاجة لحساب مسبق.
- **طريقة الإثبات:** محاولة تسجيل حساب ببريد معروف أنه مُستخدَم فعلاً، ومراقبة الرسالة المميِّزة.
- **الإصلاح المقترح:** قبوله كمخاطرة شائعة ومقبولة تجارياً في أغلب التطبيقات (يحتاج قراراً تجارياً لا تقنياً بحتاً)، أو توحيد الرسالة إلى نص عام لا يؤكد/ينفي الوجود.
- **يحتاج تغيير Architecture؟** لا.
- **قد يسبب Regression؟** لا، لكنه يُضعِف تجربة المستخدم قليلاً (رسالة أقل دقة عند الخطأ).

#### M6 — استمعات Firestore غير محدودة العدد في عدة مواضع

- **الملفات والتفصيل:**
  | الملف | الدالة | التأثير |
  |---|---|---|
  | `lib/features/products/data/repositories/firestore_product_repository.dart:89` | `watchProducts()` | كامل كتالوج المنتجات بلا `.limit()` — يكبر مع نمو المتجر |
  | `lib/features/products/data/repositories/firestore_review_repository.dart:27-30` | `watchReviews(productId)` | كل مراجعات منتج واحد بلا حدّ — منتج شهير بمراجعات كثيرة يحمّل الكل دفعة واحدة |
  | `lib/shared/services/order_service.dart:81-84` | `watchOrders(userId)` | كل طلبات عميل واحد بلا حدّ (شاشة "طلباتي") |
  | `lib/features/admin/data/repositories/firestore_admin_repository.dart:100-103` | `watchNotifications()` (إدارة) | سجل كل الإشعارات المُرسَلة تاريخياً بلا حدّ |
  | `lib/shared/services/chat_service.dart:30-33` | `watchAllChats()` (إدارة) | كل محادثات الدعم لكل العملاء بلا حدّ |
  | `lib/shared/services/refund_service.dart:70-73` | `watchAllRequests()` (إدارة) | كل طلبات الاسترداد تاريخياً بلا حدّ |
- **الأثر:** أداء وتكلفة قراءة تتدهوران تدريجياً مع نمو البيانات، لا انفجار فوري — مخاطرة أداء/تكلفة لا أمنية مباشرة.
- **هل يُستغَل من خارج التطبيق؟** لا (استعلامات تحتاج صلاحية القراءة نفسها المحدودة أصلاً بالمالك/الإدارة عبر القواعد؛ المخاطرة نمو طبيعي لا هجوم).
- **طريقة الإثبات:** مراقبة زمن تحميل هذه الشاشات مع نمو عدد المستندات فعلياً.
- **الإصلاح المقترح:** إضافة `.limit()` بقيمة معقولة لكل حالة، بنفس النمط المُطبَّق فعلاً على `watchAllOrders`/`pointsLedger`.
- **يحتاج تغيير Architecture؟** لا — تعديل سطر واحد لكل حالة.
- **قد يسبب Regression؟** منخفض جداً.

#### M7 — تبعيتان غير مُستخدَمتَين لا تزالان معلَنتين (سبب تحذير Kotlin Gradle الظاهر في كل بناء)

- **الملف:** `pubspec.yaml` (`cloud_functions: ^6.3.6`, `firebase_storage: ^13.4.6`)
- **السيناريو:** `firebase_storage` غير مُستورَدة في أي ملف `lib/` إطلاقاً (تأكَّدتُ بالبحث). `cloud_functions` مُستورَدة في ملف واحد فقط (`email_verification_service.dart`) وهو نفسه معطَّل الوظيفة فعلياً (انظر L3).
- **الأثر:** حجم APK إضافي بلا داعٍ، صلاحيات/سطح هجوم زائد بلا فائدة، وهما السبب المباشر لتحذير البناء المتكرر: *"Future versions of Flutter will fail to build if your app uses plugins that apply KGP"* — مخاطرة توافق مستقبلية حقيقية إن لم تُعالَج قبل ترقية Flutter لاحقاً.
- **هل يُستغَل من خارج التطبيق؟** لا.
- **طريقة الإثبات:** `grep -rn "firebase_storage" lib/` (فارغ)، ومراقبة رسالة تحذير `flutter build` الظاهرة فعلياً في كل بناء نُفِّذ هذه الجلسة.
- **الإصلاح المقترح:** إزالة التبعيتين من `pubspec.yaml` (وحذف/تعطيل `email_verification_service.dart` أو إصلاحه فعلياً أولاً — مرتبط بـ L3).
- **يحتاج تغيير Architecture؟** لا.
- **قد يسبب Regression؟** لا يُتوقَّع (كلاهما غير مُستخدَم فعلياً)، لكن **هذا تعديل كود فلا يجوز تنفيذه في هذه المرحلة** بناءً على تعليماتك — يُذكَر هنا كتوصية للمرحلة القادمة فقط.

---

### 🟢 Low

| ID | الوصف | ملاحظة |
|---|---|---|
| L1 | لا رؤوس أمان HTTP قياسية (`X-Content-Type-Options`, `CSP`) على استجابات push-server | أثر منخفض — واجهات JSON بحتة لا HTML، لا يستهلكها متصفح مباشرة |
| L2 | `console.error` يسجّل كائن الخطأ الخام كاملاً في سجلات Vercel | لم أجد تسريب سرّ فعلي، لكن ممارسة أفضل هي تسجيل رسالة مُلخَّصة لا الكائن كاملاً |
| L3 | `email_verification_service.dart` يستدعي دالتين (`sendEmailVerificationCode`/`verifyEmailCode`) غير منشورتين إطلاقاً | كود معطَّل، غير قابل للتنفيذ لأنه غير قابل للوصول عملياً حالياً (البوابة مُعطَّلة)، لكنه سيفشل فوراً لو أُعيد تفعيل الشاشة قبل بناء بديل حقيقي |
| L4 | لا يوجد مسار لإلغاء الطلب من طرف العميل نفسه (`cancelOrder` حُذف في مرحلة سابقة) | فجوة اكتمال ميزة، ليست ثغرة أمنية |
| L5 | حقول `notes`/`shippingAddress` تُخزَّن كنص خام بلا تنظيف HTML | غير قابل للاستغلال حالياً (Flutter `Text` لا يفسّر HTML)؛ يصبح خطراً فقط لو بُنيت لوحة إدارة ويب تعرض هذه الحقول عبر `innerHTML` مستقبلاً |
| L6 | لا يوجد `vercel.json` — كل دوال push-server تعتمد على إعدادات Vercel Hobby الافتراضية (مهلة 10 ثوانٍ) | كافٍ للحمل الحالي؛ قد يحتاج ضبطاً صريحاً لو كبرت استعلامات إحصاءات الكوبون/المراجعات مستقبلاً |

---

## B) الأشياء التي تم اختبارها ونجحت

هذه ليست افتراضات — كل بند هنا إما **جُرِّب فعلياً على Production الآن** (قراءة/محاولات مرفوضة فقط، بلا أي كتابة) أو **تُتُبِّع في الكود مباشرة** حتى نقطة التنفيذ الفعلية:

1. **تزوير المخزون/المبيعات مباشرة:** محاولة `PATCH products/p1` لتعديل `stock`/`soldCount` بلا مصادقة → `403 PERMISSION_DENIED` (فحص حيّ الآن، ونتيجة مطابقة لبطارية الاختبار الكاملة (9 محاولات) في المرحلة 11 بلا أي أثر جانبي).
2. **تزوير `coupons.usedCount`:** نفس النتيجة — مرفوض.
3. **إنشاء طلب مباشرة من Firestore:** `orders create: if false` — مرفوض بنيوياً بغض النظر عن محتوى الطلب.
4. **تزوير `userId`/`total`/`status` داخل الطلب:** الخادم لا يقرأ أياً منها من جسم الطلب إطلاقاً (تتبَّعت الكود سطراً بسطر في `order-input.js`/`order-core.js`) — `userId` يُشتَق حصراً من توقيع Google على رمز الهوية.
5. **زيادة النقاط ذاتياً:** لا مسار كتابة واحد يسمح للعميل بزيادة `points` — القاعدة تمنعها بنيوياً، والخادم فقط يُنقِصها ضمن سقف الرصيد الفعلي.
6. **قراءة إشعار عميل آخر:** مرفوض (تحقَّق منها في المرحلة 11 بمحاكي حقيقي 53/53، وأُعيد تأكيدها ضمنياً الآن بأن القواعد المنشورة لم تتغيّر).
7. **تجاوز المخزون تحت التزامن (Race Condition):** طلبان متزامنان على آخر وحدة مخزون — كلاهما يقرأ ويكتب **نفس** مستند المنتج ضمن معاملة Firestore حقيقية، فيفشل أحدهما حتماً بـ`ABORTED`/409 (خاصية Firestore الذرّية، لا اعتماد على منطق تطبيقي هش) — تتبَّعت هذا في الكود، وهو نفس السبب الذي يحمي حد استخدام الكوبون العام وحد الاستخدام لكل مستخدم من التجاوز تحت التزامن أيضاً.
8. **تكرار الطلب (Idempotency):** معرّف الطلب مُشتَقّ حتمياً من (uid + checkoutId) + شرط `exists:false` + معالجة صريحة لتصادم الكتابة المتزامنة — تتبَّعت المسارين (الفحص المسبق، وفشل الـcommit اللاحق) وكلاهما يُرجع الطلب الموجود بدل الكتابة المزدوجة.
9. **بوابة الإدارة على مستوى الواجهة قابلة للتجاوز لكن غير خطيرة:** حتى لو عدَّل أحد التطبيق ليتجاوز `redirect` الخاص بمسارات `/admin`، أي عملية قراءة/كتابة فعلية للإدارة تصطدم بـ`isAdmin()` في القواعد نفسها — تحققت من أن الاستعلام غير المُصفَّى على `orders` (لو استُخدِم من غير مدير) يُرفَض كلياً بنفس آلية "التقييم لكل مستند" التي أثبتناها في المرحلة 11.
10. **لا أسرار مسرَّبة في Git:** فحصت كامل تاريخ commits لـ`push-server` (المجلد الوحيد الخاضع لنظام إصدار في هذا المشروع) — لا ملف `.env`، لا مفتاح حساب خدمة، لا سر تم رفعه في أي وقت عبر كل التاريخ. المستودع نفسه على GitHub **خاص** (تحقَّقت: طلب API غير مصادَق عليه يُرجع 404، السلوك القياسي لمستودع خاص).
11. **لا مفتاح حساب خدمة (Service Account Private Key) داخل أي كود عميل:** بحث شامل عبر `lib/` و`push-server/api`+`lib/` — النمط الوحيد الموجود هو قراءة `sa.private_key` من متغيّر بيئة وقت التشغيل (`process.env.FIREBASE_SERVICE_ACCOUNT`)، لا قيمة مضمَّنة.
12. **مفتاح Firebase API العام (`AIzaSy...`) في `google-services.json`:** فحصته — هذا مُصمَّم ليكون علنياً حسب نموذج أمان Firebase نفسه (يُعرِّف المشروع فقط، لا يمنح صلاحية بذاته؛ الحماية الفعلية هي القواعد). **ليس ثغرة.**

---

## C) الأشياء التي لم يمكن اختبارها

1. **قيود مفتاح Firebase API الفعلية في Google Cloud Console** (هل مُقيَّد بحزمة التطبيق/بصمة SHA؟) — لا صلاحية وصول للكونسول.
2. **الاستهلاك الفعلي الحالي لحصة قراءة Firestore اليومية** (كم تبقى فعلياً من الحصة المجانية اليوم) — لا صلاحية وصول للفوترة/الكونسول.
3. **هل Firebase App Check أو أي حماية WAF مفعَّلة فعلاً من جهتكم خارج ما يظهر بالكود؟** — لا صلاحية وصول.
4. **السلوك الفعلي تحت حمل حقيقي (اختبار ضغط/تحميل)** — لم أُنفِّذ أي اختبار تحميل فعلي على Production تجنباً لأي تكلفة أو تعطيل حقيقي.
5. **هل قيم `functions.config()` (مفاتيح Stripe/Cloudinary/Gmail القديمة) لا تزال مضبوطة فعلياً على مشروع Firebase؟** (لن تؤثر إلا لو نُشرت `functions/` مستقبلاً) — لا صلاحية وصول لإعدادات Functions.
6. **دقيقة إعدادات متغيّرات بيئة Vercel** (نطاق Production مقابل Preview، من يملك صلاحية تعديلها) — لا صلاحية وصول للوحة Vercel.
7. **من يملك صلاحية وصول فعلية على مستودع GitHub الخاص لـpush-server (متعاونون آخرون؟)** — لا صلاحية وصول لإعدادات المستودع.

---

## D) المخاطر المتبقية (ملخص تنفيذي)

الأهم أولاً:

1. **سر إشعارات Push قابل للاستخراج (C1)** — أكبر ثغرة فردية، تسمح بانتحال هوية المتجر في إشعارات لعملاء حقيقيين.
2. **لا حماية من استنزاف حصة Firestore المجانية (H1)** — أسهل وأرخص طريقة لتعطيل المتجر بالكامل ليوم كامل، بلا أي حساب أو تسجيل دخول.
3. **لا تحديد معدّل على أي نقطة نهاية (H2) ولا سقف طلبات لكل حساب (H3)** — يفتح الباب لاستنزاف مخزون حقيقي بتكلفة صفرية على المهاجم (COD).
4. **سهولة تعدد الحسابات (H4)** طالما التحقق من البريد معطَّل — يُضعِف كل ضوابط "لكل مستخدم" في نظام الكوبونات.
5. مخاطر متوسطة قابلة للتأجيل النسبي: عدم التحقق من إبطال الجلسة (M1)، حماية رفع الصور غير الموقَّع (M2)، الكود الميت الخطير لو نُشر بالخطأ مستقبلاً (M3).

---

## E) التوصية: هل المتجر جاهز للإطلاق؟

**جاهز للإطلاق المحدود/التجريبي (soft launch) بعملاء معروفين وحجم منخفض — غير جاهز بعد لإطلاق عام واسع أو حملة تسويقية.**

**السبب:** الطبقة الأهم فعلياً — صحة السعر، المخزون، الهوية، منع التكرار، وصلاحيات القراءة/الكتابة — **مُختبَرة وصحيحة فعلياً**، حتى تحت التزامن، وتحققتُ منها الآن حيّاً على Production لا نظرياً فقط. هذا هو الأساس الذي لا يجوز إطلاق أي متجر بدونه، وهو موجود وسليم.

لكن الفجوات المتبقية (C1 وH1-H4 تحديداً) كلها من نوع **"رخيصة التنفيذ من مهاجم خارجي، مكلفة الأثر على المتجر"** — ولا تحتاج مهاجماً متمرّساً، فقط شخصاً يعرف كيف يفكّك APK أو يكتب سكربت `curl` بسيط. بحجم عملاء منخفض ومعروف حالياً، احتمال استهدافها منخفض عملياً — لكنها ستصبح مصدر قلق حقيقي مع أي نمو أو انتشار.

**التوصية العملية:** أطلق بحذر لعملاء محدودين الآن إن أردت، لكن عالج **C1 وH1** تحديداً (الأرخص تنفيذاً والأخطر أثراً) قبل أي إعلان عام أو حملة تسويقية موسَّعة.

---

**توقفت هنا كما طلبت. بانتظار توجيهك — لم يُعدَّل أي ملف، ولم تُرقَّ أي تبعية، ولم تُغيَّر أي قاعدة أو API.**
