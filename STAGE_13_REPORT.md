# تقرير المرحلة 13 — Production Security Hardening

**التاريخ:** 2026-08-20
**النطاق:** تنفيذ فعلي للإصلاحات المتفق عليها بالترتيب المطلوب (C1 → H1 → H2 → H3 → H4 → M1/M2/M6 → تنظيف).
**القاعدة الملتزَم بها طوال المرحلة:** لا بيانات Production حقيقية مُسّت، لا حذف، لا ترقية تبعيات بلا سبب أمني، لا تغيير عشوائي في Firestore Rules (لم تُلمَس إطلاقاً هذه المرحلة).

---

## 1) الملفات التي تم تعديلها

### Flutter (`lib/`)
| الملف | التغيير |
|---|---|
| `lib/core/constants/push_config.dart` | حذف `sharedSecret` نهائياً |
| `lib/shared/services/push_trigger_service.dart` | إعادة كتابة: يرسل `Authorization: Bearer <Firebase ID Token>` بدل السرّ الثابت، بمُنشئ اختباري قابل للحقن |
| `lib/main.dart` | تفعيل Firebase App Check (وضع Debug/غير مُلزِم) بعد `Firebase.initializeApp()`، بلا انتظار وبلا تأثير على الإقلاع |
| `pubspec.yaml` | إضافة `firebase_app_check: ^0.4.1`، حذف `firebase_storage` (غير مُستخدَمة إطلاقاً) |
| `lib/features/products/data/repositories/firestore_product_repository.dart` | `watchProducts()`: أضيف `.limit(500)` |
| `lib/features/products/data/repositories/firestore_review_repository.dart` | `watchReviews()`: أضيف `.limit(200)` |
| `lib/shared/services/order_service.dart` | `watchOrders()`: أضيف `.limit(100)` (قابل للتخصيص) |
| `lib/features/admin/data/repositories/firestore_admin_repository.dart` | `watchNotifications()`: أضيف `.limit(200)` |
| `lib/shared/services/chat_service.dart` | `watchAllChats()`: أضيف `.limit(200)` |
| `lib/shared/services/refund_service.dart` | `watchAllRequests()`: أضيف `.limit(200)` |
| `firebase.json` | حذف مفتاح `functions` (منع نشر `functions/` بالخطأ مستقبلاً) |
| `functions/index.js` | إضافة تحذير بارز أعلى الملف فقط — **لم يُحذف أي سطر** |

### الخادم (`push-server/`)
| الملف | التغيير |
|---|---|
| `api/notify.js` | إعادة كتابة كاملة: تحقق من رمز هوية Firebase بدل السرّ الثابت، صلاحية مبنية على `role` من Firestore، حدّ معدّل مزدوج (مدير/عادي) |
| `api/order.js` | إضافة حدّ معدّل (10/دقيقة) + سقف 3 طلبات pending لكل مستخدم داخل نفس المعاملة الذرّية |
| `api/review.js` | إضافة حدّ معدّل (5/دقيقة) |
| `lib/permissions.js` | **جديد** — `isAdmin(db, uid)`: قراءة دور المستخدم الحقيقي من Firestore |
| `lib/notify-core.js` | **جديد** — منطق قرار صلاحية الإشعار (دالة نقية مُختبَرة) |
| `lib/rate-limit.js` | **جديد** — محدِّد معدّل في الذاكرة (بلا Firestore، بلا تبعية خارجية) |
| `lib/firestore.js` | `query()` أصبحت تقبل معامل `transaction` اختياري |
| `lib/pricing.js` | إضافة `MAX_PENDING_ORDERS_PER_USER` و`assertPendingOrderLimit()` |
| `package.json` | تحديث سكربت الاختبار ليشمل الملفات الجديدة |

### اختبارات جديدة
`push-server/test/notify-core.test.js` (7) · `push-server/test/permissions.test.js` (5) · `push-server/test/rate-limit.test.js` (6) · إضافات على `push-server/test/pricing.test.js` (4) · `test/services/push_trigger_service_test.dart` (4)

---

## 2) كل ثغرة تم إغلاقها + مستوى الخطورة

| المعرّف | الثغرة | الخطورة | الحالة |
|---|---|---|---|
| **C1** | سرّ إشعارات ثابت داخل APK الموزَّع، قابل للاستخراج، يسمح بانتحال هوية المتجر | 🔴 حرجة | **مُغلقة بالكامل** — لا سرّ ثابت بعد الآن، مُختبَرة حيّاً على Production |
| **H1** | لا حماية من استنزاف حصة Firestore المجانية اليومية من خارج التطبيق | 🟠 عالية | **الكود جاهز ومنشور** (App Check بوضع Debug) — **الإنفاذ الفعلي (Enforcement) لم يُفعَّل بعد**، يتطلب إعداداً في Firebase Console (تفصيل في القسم 12) |
| **H2** | لا تحديد معدّل على أي نقطة نهاية — يفتح الباب لاستنزاف مخزون/تكلفة | 🟠 عالية | **مُغلقة** — حدّ معدّل حقيقي على الطلبات الثلاث، مُختبَر منطقياً (103 اختبار)، **غير مُختبَر عبر HTTP حقيقي على Production** (يحتاج توكناً صحيحاً، راجع القسم 8) |
| **H3** | لا سقف على عدد طلبات pending لكل مستخدم | 🟠 عالية | **مُغلقة** — سقف 3، محمي من التزامن عبر ضمان معاملات Firestore الذرّية نفسه المستخدَم أصلاً لحماية المخزون |
| **H4** | سهولة تعدد الحسابات (Sybil) لاستغلال كوبونات العملاء الجدد | 🟠 عالية | **غير مُصلَحة عمداً** — فُحصت بالكامل، القرار: لا تُنفَّذ الآن (تفصيل في القسم 6) |
| **M1** | لا تحقق من إبطال (Revocation) رمز الهوية | 🟡 متوسطة | **قرار واعٍ بعدم التنفيذ الآن** — السبب موثَّق في القسم 7 |
| **M2** | Cloudinary Unsigned Preset قابل للاستغلال من خارج التطبيق | 🟡 متوسطة | **لم يُنفَّذ كود** — توصيات إعداد Cloudinary موثَّقة في القسم 9 (تتطلب إجراءً من طرفك في كونسول Cloudinary) |
| **M6** | 6 استمعات Firestore غير محدودة | 🟡 متوسطة | **مُغلقة بالكامل** — حدود مضافة للستة جميعاً |
| **M7** | تبعيتان ميتتان (`cloud_functions`, `firebase_storage`) | 🟢 منخفضة | **`firebase_storage` أُزيلت**. **`cloud_functions` بقيت عمداً** (مرتبطة بقرار H4 المؤجَّل) |
| **M3** | كود Cloud Functions قديم (Stripe + placeOrder قديم) قد يُنشر بالخطأ | 🟡 متوسطة | **معزول** — أُزيل الربط من `firebase.json`، وأُضيف تحذير بارز في الملف نفسه، **لم يُحذف شيء** |

---

## 3) الاختبارات التي تم تنفيذها

### منطقية (Unit) — كل الادعاءات الأمنية أدناه مبنية على تتبع كود فعلي أو اختبار فعلي، لا افتراض

- **السرّ القديم مرفوض:** اختبار حيّ على Production (القسم 8، البند 3).
- **رمز مفقود / مزوّر:** اختبار حيّ على Production بتوكن حقيقي البنية (JWT صحيح الشكل، توقيع وهمي) — رُفض عند مرحلة التحقق من الشهادة (`unknown_kid`)، لا مجرد فحص شكلي سطحي.
- **عميل عادي لا يرسل لعميل آخر / مدير يرسل لأي أحد:** مُختبَر منطقياً في `notify-core.test.js` (7 اختبارات تغطي: إشعار ذاتي، حظر استهداف الآخرين لغير المدير، سماح المدير، حظر البثّ العام لغير المدير، مقاومة قيم `isCallerAdmin` المزوَّرة نوعياً).
- **صلاحية المدير من مصدر موثوق:** `permissions.test.js` — يتحقق أن القرار يعتمد حصراً على `role` الحقيقي من Firestore (بما في ذلك رفض قيم شبيهة بالحروف الكبيرة `"Admin"` كتلاعب محتمل).
- **حدّ المعدّل → 429 → يعود للعمل بعد النافذة:** `rate-limit.test.js` (6 اختبارات) — يثبت القبول ضمن الحد، الرفض بعده مع `retryAfterSeconds` موجب، عودة العمل بعد انتهاء النافذة، وعزل المستخدمين عن بعضهم.
- **سقف الطلبات pending:** `pricing.test.js` (4 اختبارات جديدة) — يثبت القبول تحت الحد، الرفض عند الحد وفوقه بالضبط.
- **Idempotency لا تزال تعمل:** لم تُمَس آلية `deriveOrderId`/`exists:false` إطلاقاً — الفحص الجديد (pending limit) يقع **بعد** فحص التكرار في `order.js`، فإعادة إرسال نفس `checkoutId` لا تصل له أصلاً (تُكتشَف كتكرار وتُعاد فوراً).
- **تزوير السعر/الإجمالي/المخزون/`userId`/`usedCount`:** لم يتغيّر أي من `order-core.js`/`pricing.js`/`order-input.js` المسؤولة عن هذا — كل الاختبارات القديمة (61 من مجموعها) لا تزال تمرّ بلا تعديل، مما يثبت عدم وجود أي أثر جانبي على هذا الجزء.
- **PushTriggerService لم يعد يحمل السرّ:** `push_trigger_service_test.dart` (4 اختبارات) — يثبت إرسال `Authorization: Bearer`، غياب `x-shared-secret` تماماً، عدم إرسال أي طلب بلا رمز هوية.

### npm test (push-server)
```
103/103 ناجح
```
(كانت 93 قبل هذه المرحلة — أُضيف 10 اختباراً جديداً بالضبط كما هو موثَّق أعلاه)

### flutter test
```
201/201 ناجح
```
(كانت 197 — أُضيف 4 اختبارات جديدة)

---

## 4) نتائج flutter analyze

```
6 issues found (0 errors)
```
كلها `info` تجميلية — 5 كانت موجودة مسبقاً قبل هذه المرحلة، وواحدة جديدة (`prefer_initializing_formals` على `push_trigger_service.dart`، نفس النمط التجميلي المقبول أصلاً في ملفين آخرين بالمشروع). **صفر أخطاء.**

## 5) نتائج flutter test

```
201/201 ناجح
```
مع بناء APK فعلي كامل (`flutter build apk --debug`) نجح دون أخطاء — يثبت أن تكامل حزمة `firebase_app_check` الجديدة على مستوى Android سليم.

## 6) نتائج npm test

```
103/103 ناجح
```

---

## 7) القرار: لماذا لم يُصلَح H4 (Sybil/تعدد الحسابات) الآن

**الفحص الذي تم:**
- `email_verification_service.dart` يستدعي دالتين (`sendEmailVerificationCode`, `verifyEmailCode`) عبر `cloud_functions` — كلتاهما **غير منشورتين إطلاقاً** (المشروع على خطة Spark التي لا تدعم Cloud Functions أصلاً). أي استدعاء لهما سيفشل فوراً.
- شاشة التحقق (`email_verification_screen.dart`) **غير قابلة للوصول** من أي مكان في التطبيق حالياً — لا زر، لا رابط، لا تنقّل يدوي إليها (تحقّقت بالبحث الكامل في الكود). المسار الوحيد إليها كان بوابة إعادة التوجيه في `routes.dart`، وهي مُعطَّلة (`requireEmailVerification = false`).
- **لماذا التحقق غير عامل:** ليس خللاً بسيطاً — الآلية بأكملها مبنية على Cloud Functions التي لا يمكن نشرها على الخطة الحالية. إعادة `requireEmailVerification = true` وحدها ستُعطِّل تسجيل الدخول فعلياً لأي مستخدم جديد (لن يصل الكود أبداً).

**القرار: لم أُعِد التفعيل، ولم أبنِ نظاماً بديلاً — كما طلبت صراحة.**

**التوصية لإغلاق Sybil مستقبلاً (لمرحلة مستقلة):** نفس نمط `/api/order`/`/api/review` بالضبط — نقطتا نهاية جديدتان على `push-server`:
1. `/api/send-verification-code`: تتحقق من رمز الهوية، تولّد كوداً من 6 أرقام، تخزّنه في `emailVerificationCodes/{uid}` (مجموعة **موجودة أصلاً** في قواعد Firestore ومحجوبة كلياً عن العميل: `allow read, write: if false` — جاهزة تماماً لهذا الغرض)، وترسل الكود عبر خدمة بريد مجانية لا تحتاج تبعية (مثل REST API لـ Resend.com، بنفس فلسفة "بلا تبعيات" المتبعة في بقية push-server).
2. `/api/verify-email-code`: تتحقق من تطابق الكود المُرسَل، وتكتب `users/{uid}.emailVerified = true` بامتياز حساب الخدمة.
3. عندئذ فقط تُعاد `requireEmailVerification = true` بأمان.

هذا لم يُنفَّذ في هذه المرحلة كما طلبت — مطروح فقط كخيار جاهز لمرحلة قادمة.

---

## 8) القرار: لماذا لم يُنفَّذ M1 (Token Revocation) الآن

**التقييم:**
- التنفيذ الكامل يتطلب استدعاء `identitytoolkit.googleapis.com/v1/accounts:lookup` للتحقق من `validSince` — هذا **نطاق OAuth جديد** (`identitytoolkit`) يُضاف لنفس دالة `getAccessToken()` المشتركة بين FCM وFirestore، فيوسّع صلاحيات التوكن المُستخدَم في كل الخادم، لا في نقطة واحدة فقط.
- استدعاء شبكة إضافي **على كل طلب** بلا تخزين مؤقت يعني زيادة زمن استجابة ملموسة على مسار الدفع تحديداً (حيث السرعة مهمة للمستخدم).
- **الخطر الذي يعالجه محدود زمنياً بطبيعته أصلاً:** حتى بلا هذا الفحص، أي توكن مسروق/باقٍ بعد تعطيل حساب ينتهي صلاحيته تلقائياً خلال ساعة كحد أقصى (عمر توكن Firebase الطبيعي) — هذه ليست ثغرة مفتوحة بلا حدّ، بل نافذة زمنية ضيّقة ومحدودة سلفاً.

**القرار: لم يُنفَّذ الآن.** التعقيد المُضاف (نطاق صلاحية جديد يشمل الخادم بأكمله + زمن استجابة إضافي دائم) غير متناسب مع مخاطرة محدودة زمنياً بساعة واحدة كحد أقصى أصلاً. **لم تُضعَف أي طبقة من التحقق الحالي** (الخوارزمية، `kid`، التوقيع، `aud`، `iss`، `exp`، `sub` — كلها كما هي بلا مساس).

**إن أردتَه مستقبلاً:** الخيار الأنسب هو تخزين مؤقت (Cache) لنتيجة `accounts:lookup` لكل `uid` لمدة 2-5 دقائق داخل نفس آلية `lib/rate-limit.js` الموجودة أصلاً (بنية شبيهة جاهزة) — يقلّص التأخير الإضافي لمرة كل بضع دقائق لكل مستخدم بدل كل طلب.

---

## 9) M2 — توصيات Cloudinary (لم تُنفَّذ، تتطلب إجراءً منك في كونسول Cloudinary)

الكود (`storage_service.dart`) **لم يتغيّر** — طريقة الرفع الحالية تعمل كما هي تماماً. المطلوب منك فقط في **Cloudinary Console → Settings → Upload → إعدادات الـ Upload Preset `alarabi_crystal`**:

| الإعداد | القيمة الموصى بها |
|---|---|
| Max file size | ~5 ميجابايت (يكفي لصور منتجات بجودة جيدة) |
| Allowed formats | `jpg, png, webp` فقط |
| Folder restriction | تقييد الرفع على مجلدات `products/`, `avatars/`, `reviews/` فقط (نفس المسارات التي يستخدمها التطبيق فعلاً) |
| Moderation | تفعيل الفحص التلقائي إن كان متاحاً في خطتكم |

**الحل الجذري (غير منفَّذ، يحتاج قراراً منك):** نقل الرفع لمسار موقَّع عبر `push-server` (نقطة نهاية جديدة تتحقق من رمز الهوية ثم توقّع طلب الرفع). هذا تغيير معماري حقيقي (يمسّ كل شاشات رفع الصور) — لم أُنفّذه لأنه لم يكن ضرورياً لهذه المرحلة، فقط أذكره كخيار.

---

## 10) اختبارات Production الآمنة التي نُفِّذت فعلياً

كل ما يلي حقيقي، نُفِّذ الآن، بلا أي كتابة أو تأثير على بيانات حقيقية:

| الاختبار | النتيجة |
|---|---|
| `/api/notify` بلا توكن | `401` |
| `/api/notify` بتوكن مزوَّر (شكل صحيح، توقيع/`kid` وهميان) | `401 unknown_kid` |
| `/api/notify` بالسرّ القديم فقط (بلا Authorization) | `401 missing_token` — **يثبت أن السرّ القديم لم يعد يعمل إطلاقاً** |
| `/api/order` بلا توكن | `401` |
| `/api/review` بلا توكن | `401` |
| `/api/order` بتوكن مزوَّر مماثل، محاولة استهداف مستخدم آخر عبر `/api/notify` | `401 unknown_kid` (رُفض قبل الوصول لأي فحص صلاحية) |
| `GET` بدل `POST` على `/api/order` | `405` |
| `GET` بدل `POST` على `/api/notify` | `405` |

---

## 11) أي Regression محتمل

1. **إشعارات فئة الكوبون المستهدفة الكبيرة (`coupon_admin_service.dart`):** الحلقة التي ترسل إشعاراً لكل عميل مطابق تستدعي `/api/notify` بالتتابع. حدّ المدير الجديد (100/دقيقة) سخيّ، لكن لو تجاوزت فئة العملاء المطابقين 100 عميل خلال نفس الدقيقة، ستفشل الإشعارات الزائدة **صامتة** (نفس سلوك `PushTriggerService` الأصلي عند أي فشل شبكة). **هذا قيد معروف موثَّق، لا افتراض** — بحجم عملاء المتجر الحالي (فحصت البيانات الفعلية سابقاً، عدد قليل جداً) لا يُشكّل خطراً عملياً الآن، لكن يستحق المتابعة لو نما عدد العملاء بشكل كبير.
2. **App Check ووضع Debug:** حتى يُسجَّل رمز Debug Token في Firebase Console، طلبات App Check ستفشل بصمت (بلا أي تأثير لأن Enforcement غير مفعَّل) — لا Regression فعلي، لكن يعني أن الحماية غير فعّالة عملياً حتى تُكمِل الإعداد (تفصيل في القسم 12).
3. **لا Regression آخر مكتشَف** في أي من الاختبارات الشاملة (201 Flutter + 103 خادم) — جميعها اجتازت دون أي فشل.

---

## 12) ما تبقّى بدون إصلاح / يحتاج إجراءً منك

### لإكمال H1 (App Check) فعلياً:
1. افتح **Firebase Console → App Check**، سجّل تطبيق Android.
2. **لا تُفعِّل Enforcement بعد.** اترك الوضع الافتراضي (Unenforced/Monitor).
3. شغّل نسخة Debug من التطبيق على جهاز حقيقي، وابحث في سجل التشغيل (logcat أو مخرجات `flutter run`) عن سطر يبدأ بـ"Enter this debug secret into the allow list" — انسخ القيمة.
4. أضِف هذه القيمة في **App Check → Apps → (تطبيقكم) → Manage debug tokens**.
5. راقب تبويب **Metrics** لبضعة أيام (يُظهر نسبة الطلبات المصحوبة برمز App Check صالح مقابل المرفوضة) — **فقط بعد رؤية أن التطبيق الحقيقي يمرّ بنجاح**، فكّر بتفعيل Enforcement (ويُفضَّل تدريجياً على Firestore فقط أولاً، لا كل الخدمات دفعة واحدة).

### لم يُختبَر (بصراحة تامة، لا ادّعاء):
- **429 عبر HTTP حقيقي على Production:** يتطلب توكن هوية Firebase صالحاً لا أملكه ولن أطلبه منك (قيد التعامل مع بيانات الاعتماد الثابت طوال الجلسة). المنطق نفسه مُختبَر بدقة (6 اختبارات وحدة) ومُتتبَّع في الكود سطراً بسطر.
- **نجاح إشعار موجَّه/بثّ عام من حساب مدير حقيقي:** نفس السبب — يحتاج توكن مدير حقيقي.
- **فعالية App Check الحقيقية على جهاز فعلي:** يحتاج جلسة مباشرة على جهازك (القسم أعلاه).
- **قيود مفتاح Cloudinary/Firebase API الفعلية في الكونسول:** لا صلاحية وصول لأي كونسول خارجي.
- **الأثر الفعلي لحدّ الطلبات pending تحت تزامن حقيقي على Production:** الضمان نظري (موثَّق ومُختبَر منطقياً عبر خاصية Firestore الذرّية نفسها المُستخدَمة للمخزون)، غير مُختبَر بطلبات حقيقية متزامنة فعلياً (كان سيتطلب إنشاء طلبات حقيقية، ممنوع بلا موافقتك الصريحة).

---

## 13) مستوى الثقة الأمني لكل جزء

| الجزء | الثقة | لماذا |
|---|---|---|
| هوية المتصل في الطلبات الثلاث (order/review/notify) | **عالية** | توقيع RS256 حقيقي يُتحقَّق منه فعلياً — أثبتنا هذا حيّاً بتوكن مزوَّر مرفوض عند فحص التوقيع نفسه، لا فحصاً سطحياً |
| صلاحية المدير في `/api/notify` | **عالية** | مصدرها قراءة Firestore حقيقية بامتياز حساب خدمة، لا قيمة من العميل — مُختبَرة منطقياً بدقة |
| حماية السعر/المخزون/الكوبون/الهوية في الطلبات | **عالية (بلا تغيير)** | لم تُمَس هذه المرحلة إطلاقاً — كل اختبارات المرحلتين 10/11 (61 اختباراً) لا تزال تمرّ حرفياً |
| حدّ المعدّل (Rate Limiting) | **متوسطة** | المنطق صحيح ومُختبَر بدقة، لكنه **في الذاكرة لا موزَّع** — هجوم بطيء عبر نسخ Vercel باردة متعددة قد يتفلّت جزئياً، موثَّق صراحة في الكود |
| سقف الطلبات pending | **عالية منطقياً، غير مُختبَرة على Production حقيقياً** | يعتمد على نفس ضمان Firestore الذرّي المُثبَت أصلاً لحماية المخزون |
| App Check | **غير فعّالة بعد** | جاهزة بالكود، تنتظر إكمالاً في الكونسول من طرفك |
| Sybil/تعدد الحسابات | **لا حماية** | قرار مؤجَّل صراحة بطلبك |
| Token Revocation | **لا حماية إضافية (نافذة ساعة واحدة كحدّ أقصى)** | قرار واعٍ بعدم التنفيذ، موثَّق |
| Cloudinary | **كما كانت** | لم يتغيّر الكود، التوصيات بانتظار تنفيذك في الكونسول |

**لا أقول إن النظام آمن 100%، ولا إن الكود خالٍ من كل الثغرات.** كل ادّعاء أعلاه إمّا اختبار حقيقي نُفِّذ الآن، أو تتبّع كود مباشر، أو قرار محدَّد بسببه بوضوح.

---

## هل التطبيق جاهز للإطلاق العام الآن؟

**أقرب بخطوة حاسمة، لكن ليس بعد — والسبب الوحيد المتبقي هو H1 غير المُفعَّلة فعلياً.**

الثغرة الأخطر والأسهل استغلالاً في تقرير المرحلة 12 (**C1**، انتحال هوية المتجر في الإشعارات) **مُغلقة بالكامل ومُختبَرة حيّاً على Production**. ثغرتا H2/H3 (استنزاف مخزون/تكلفة عبر طلبات متكررة) **مُغلقتان بالكود** وإن كان الاختبار الكامل عبر HTTP الحقيقي غير ممكن بلا توكن حقيقي.

الفجوة المتبقية الأخطر عملياً: **H1 (استنزاف حصة Firestore) لا تزال مفتوحة فعلياً** — الكود جاهز لكن الحماية الحقيقية (Enforcement) لم تُفعَّل بعد، وهذا ينتظر إجراءً منك في Firebase Console (القسم 12) لا مني.

**توصيتي المحدَّثة:** جاهز للإطلاق المحدود بعملاء معروفين الآن (كما كان في المرحلة 12) — **أقرب بكثير للإطلاق العام** بعد إغلاق C1، لكن أكمل إعداد App Check (Debug Token + مراقبة المقاييس + تفعيل Enforcement التدريجي) قبل أي حملة تسويقية موسَّعة تزيد فعلياً من احتمال استهداف المتجر بسكربتات قراءة خارجية.
