# تقرير المراحل 3 → 9 (الخادم جاهز ومُختبَر ومنشور)

**التاريخ:** 2026-08-17
**لم أعدّل أي ملف في التطبيق. المسار القديم يعمل كما هو.**

---

## ما تم إنجازه

### المرحلة 3-4: تصميم API والتحقق من الهوية

6 ملفات جديدة في `push-server` — كلها بلا أي تبعية خارجية (وحدة `crypto` المدمجة فقط، احتراماً للقيد الذي أفشل النشر سابقاً):

| الملف | الدور |
|---|---|
| `push-server/lib/google-auth.js` | `verifyIdToken()` — يتحقق من توقيع Google على رمز الهوية: الخوارزمية، الشهادة عبر `kid`، `aud` = مشروعنا، `iss`، الصلاحية، وجود `sub`. **الهوية تُستخرج من التوقيع لا من جسم الطلب.** |
| `push-server/lib/firestore.js` | عميل Firestore REST + **معاملات حقيقية** (`beginTransaction`/`commit`) — إن عدّل أحد مستنداً قرأناه، يفشل الـ commit بـ `ABORTED` فلا يحدث بيع زائد |
| `push-server/lib/pricing.js` | كل الحساب: الكوبونات، العروض، النقاط، الشحن، الإجمالي |
| `push-server/lib/order-input.js` | الحد الفاصل: يُرجع `{productId, quantity}` **فقط** — الإهمال بنيوي لا شرطي |
| `push-server/lib/order-core.js` | بناء الطلب وقائمة الكتابات — دالة نقية بالكامل |
| `push-server/api/order.js` · `push-server/api/review.js` | نقطتا النهاية (إدخال/إخراج فقط) |

### المرحلة 5-6: الحساب والمعاملة

الخادم يقرأ الأسعار والمخزون والكوبون والعروض من Firestore بصلاحياته، يحسب
`subtotal / couponDiscount / offerDiscount / pointsDiscount / shipping / total`
بنفسه، ثم يكتب **داخل معاملة واحدة**: تخفيض المخزون + زيادة `soldCount` بنفس
المقدار + خصم النقاط + `usedCount +1` + إنشاء الطلب بشرط `exists: false`.

**خلل العروض أُصلح هنا:** الخادم يطبّق أفضل عرض نشِط ويُدخله في الإجمالي
المحفوظ. ولأنه يعمل من الآن فصاعداً فقط، **لم يُمسّ أي طلب أو مخزون أو كوبون
قديم** — لم أقرأ ولا كتبت سطراً واحداً في بيانات الإنتاج.

### المرحلة 7-8: الاختبارات — 56/56 ناجح

الأمر:

    cd push-server && npm test

| السيناريو المطلوب | النتيجة |
|---|---|
| 1. تغيير سعر المنتج | ✅ يُهمل — سعر Firestore هو المستخدم |
| 2. تغيير الإجمالي | ✅ `total: 1` المرسل يُهمل، الإجمالي 2000 |
| 3. تغيير المخزون | ✅ `stock: 99999` يُهمل، والنقص = الكمية بالضبط |
| 4. تغيير `usedCount` | ✅ يزيد 1 عن قيمة Firestore، والكتابة مقصورة على `usedCount`+`usedBy` |
| 5. طلب باسم `userId` آخر | ✅ يُكتب معرّف التوكن، ولا تُلمس بيانات الضحية |
| 6. طلب بحالة `delivered` | ✅ كل الحالات المزوّرة → `pending` |
| 7. كوبون منتهٍ | ✅ مرفوض `coupon_expired` |
| 8. تجاوز حد الاستخدام | ✅ مرفوض (عام + لكل مستخدم + الفئة المستهدفة) |
| 9. كمية أكبر من المخزون | ✅ مرفوضة — وحتى لو قُسّمت على سطرين (تُدمج أولاً) |
| 11. زيادة نقاط الولاء | ✅ الرصيد يُقرأ من Firestore، والكتابة تُنقص فقط |
| إضافي | مخزون سالب، منتج وهمي، كتابة على منتج خارج الطلب، عرض غير نشِط، حقول `trackingNumber`/`pointsAwarded` من العميل |

### المرحلة 9: النشر — مستقر

    https://alarabi-push.vercel.app/api/order
    https://alarabi-push.vercel.app/api/review

اختبار مباشر على الإنتاج:

| الاختبار | النتيجة |
|---|---|
| بلا توكن | `401 missing_token` |
| توكن مزوّر `alg:none` | `401 bad_alg` |
| توكن بمفتاح توقيع مجهول | `401 unknown_kid` |
| توكن عبثي | `401 malformed_token` |
| `GET` بدل `POST` | `405` |
| `/api/notify` (اختبار انحدار) | `401` — يعمل كما كان |
| 5 نداءات متتالية | `401` في كل مرة، ~0.49 ثانية ثابتة |

**مشكلة ظهرت وحُلَّت:** أول محاولة نشر رجعت `Not authorized` — كانت عارضة،
والمحاولة الثانية نجحت (`● Ready`). حيلة `.git` من README لا تزال ضرورية
وطُبّقت.

---

## حد واحد يجب معرفته قبل المرحلة 10

**لم تُختبر حالة النجاح (طلب حقيقي يُحفظ) على الإنتاج** — لسببين:

1. تحتاج رمز هوية Firebase حقيقياً، أي تسجيل دخول بكلمة مرور — وهذا حد ثابت
   لا يُتجاوز.
2. لو نجحت، لأنشأت طلباً حقيقياً وخفّضت مخزوناً حقيقياً — والمساس بالمخزون
   والسجلات ممنوع.

**البديل المُنفَّذ:** 24 اختباراً يفحص الكتابات الناتجة **بالضبط** (نفس الكود
الذي ينفّذه الخادم) بلا اتصال. الحرف الحقيقي الأول لحالة النجاح سيكون شراءً
فعلياً على الجوال في المرحلة 12.

---

## خطة المرحلة 10

الملفات: `lib/core/constants/push_config.dart` (رابط جديد) ·
`lib/shared/services/order_service.dart` (`placeOrder` تنادي الخادم) ·
`lib/features/cart/presentation/pages/checkout_screen.dart` (رسائل الخطأ)

**الحماية من الانحدار:** إن فشل الخادم لعطل شبكة أو خطأ 500، يرجع التطبيق
تلقائياً للمسار القديم (لا يزال مسموحاً بالقواعد الحالية) — فلا يتوقف البيع
أبداً. أما أخطاء المنطق (مخزون ناقص، كوبون منتهٍ) تُعرض للعميل ولا تُتجاوَز.

هذا الرجوع التلقائي **يُلغى في المرحلة 11** عند تشديد القواعد، ولن تُشدَّد قبل
تأكيد نجاح شراء حقيقي على الجوال.
