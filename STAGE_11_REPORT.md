# تقرير المرحلة 11 — تشديد Firestore Rules وإغلاق المسار القديم

**التاريخ:** 2026-08-18
**الحالة:** الكود مكتمل ومُختبَر بالكامل محلياً. **القواعد الجديدة لم تُنشر على Production بعد** — القسم 11 يوضّح لماذا وماذا يلزم.

---

## 1) جميع Firestore Rules التي تغيّرت

الملف: `firestore.rules`

| المجموعة | قبل | بعد |
|---|---|---|
| `products` | العميل يقدر تخفيض `stock` وزيادة `soldCount` مباشرة (بسقف 20) | **الكتابة للإدارة فقط.** لا استثناء للعميل إطلاقاً |
| `coupons` | العميل يقدر زيادة `usedCount`/`usedBy` مباشرة | **الكتابة للإدارة فقط.** لا استثناء للعميل إطلاقاً |
| `orders` | `create` مسموح للعميل بشرط `userId==uid` و`status=='pending'` | **`create: if false`** — لا إنشاء طلب من العميل إطلاقاً، مهما كانت البيانات |
| `users` | حقل `points` قابل للتعديل الذاتي بالنقصان فقط | **`points` أُزيل كلياً** من الحقول القابلة للتعديل الذاتي — لا زيادة ولا نقصان من العميل |
| `notifications` | `allow read: if isSignedIn()` — أي مستند لأي مسجَّل (**ثغرة M1**) | **قراءة كل مستند حسب استهدافه**: عام لكل مسجَّل، أو لصاحب `targetId` فقط، أو للإدارة فقط لهدف `admin`. المدير يقرأ كل شيء |
| `reviews` | العميل ينشئ/يعدّل/يحذف مراجعته مباشرة | **`write: if false`** كلياً — الكتابة عبر الخادم الموثوق فقط |

كل تغيير موثَّق بتعليق عربي داخل `firestore.rules` نفسه يشرح السبب والبديل.

---

## 2) العمليات التي أصبحت Backend-only

| العملية | المسار الجديد |
|---|---|
| إنشاء الطلب بالكامل (حجز مخزون، خصم نقاط، استهلاك كوبون، إنشاء المستند) | `push-server/api/order.js` (منذ المرحلة 10) |
| إنشاء/تعديل/حذف المراجعة + إعادة حساب `rating`/`reviewCount` | `push-server/api/review.js` (**جديد في هذه المرحلة**) |

كلا المسارين يكتبان بامتيازات حساب خدمة (Service Account OAuth Token) تتجاوز قواعد Firestore بنيوياً — وهذا بالضبط سبب أمانهما حتى بعد إغلاق كل الاستثناءات الجانبية للعميل.

---

## 3) هل تم حذف fallback بالكامل؟

**نعم.** `OrderService.submitOrder()` الآن:
- ينادي الخادم فقط، بلا أي محاولة بديلة محلية.
- أي خطأ (رفض منطقي 4xx، أو عطل تقني شبكة/5xx) يُترجم إلى `OrderException` بنص عربي واضح ويُعرض للعميل مباشرة.
- **لا يُنشئ أي طلب محلياً تحت أي ظرف.**
- إعادة المحاولة تستخدم **نفس** `checkoutId` تلقائياً لأنه حقل `final` في `CheckoutScreen` — لا يُعاد توليده بين محاولة وأخرى، فيبقى الحذر من التكرار قائماً حتى مع إعادة المحاولة اليدوية.

الملفات: `lib/shared/services/order_service.dart` (أُعيدت كتابته بالكامل — أصبح 157 سطراً بدل 333)، `lib/features/cart/presentation/pages/checkout_screen.dart`.

## 4) هل تم حذف placeOrder() القديم؟

**نعم، حُذف كلياً** — المعاملة المحلية التي كانت تكتب `stock`/`soldCount`/`points`/`usedCount` مباشرة من التطبيق لم تعد موجودة في الكود إطلاقاً (لا كدالة معطَّلة، بل حُذفت أسطرها). كل استدعاء قديم لها (بما في ذلك أداة `lib/tools/cloud_sync_test.dart` اليدوية) حُدِّث لينادي `submitOrder()` عبر الخادم.

---

## 5) هل تم إصلاح M1 (خصوصية الإشعارات)؟

**نعم، وتحديداً بثلاث طبقات:**

1. **القاعدة** (`firestore.rules`): كل مستند إشعار يُقيَّم على حدة بحسب `target`/`targetId` كما في الجدول أعلاه.
2. **العميل** (`lib/shared/services/notifications_feed.dart`، ملف جديد): بدل استعلام واحد غير مُصفّى، الآن استعلامان منفصلان يُدمجان محلياً:
   - العام: `target in ['all','category']`
   - الموجَّه لي: `target=='user' AND targetId==uid` (كلا الحقلين معاً — تفصيل مهم، انظر القسم 8)
   - المدير: استعلام واحد غير مُصفّى (مسموح له قراءة كل شيء)
3. **الفهرس** (`firestore.indexes.json`): فهرسان مركّبان جديدان — `(target, targetId, createdAt)` و`(target, createdAt)`.

الشاشات الثلاث التي كانت تقرأ المجموعة مباشرة (`notifications_screen.dart`, `notification_bell.dart`, `in_app_notification_service.dart`) حُوِّلت جميعها لاستخدام `NotificationsFeed.watch()` بدل استعلام Firestore مباشر.

---

## 6) هل تم ربط /api/review؟

**نعم.** `FirestoreReviewRepository.addReview()` (شاشة `product_details_screen.dart` لم تتغيّر إطلاقاً — الواجهة نفسها) يستدعي الآن `ReviewApi.upsert()` (ملف جديد `lib/shared/services/review_api.dart`) بدل الكتابة المباشرة في Firestore.

**إضافة تتجاوز الحد الأدنى المطلوب:** أضفت أيضاً حذف مراجعة (`ReviewApi.delete()` + `action:'delete'` في الخادم) لأن طلبك نص صراحة على اختبار أن "حذف المراجعة يعيد حساب الإحصائيات بشكل صحيح" — لم يكن هذا موجوداً سابقاً في التطبيق (لا زر حذف في أي شاشة). **الخادم يدعمه ومُختبَر بالكامل، لكن لا يوجد زر حذف في واجهة المستخدم بعد** — لم أُضِفه لأن لا أحد طلب ميزة حذف مراجعة جديدة، فقط طلبت التأكد أن الخادم يتصرف بشكل صحيح لو استُخدم.

---

## 7) نتيجة اختبارات Rules (محاكي Firebase حقيقي — ليست قراءة يدوية)

```
firestore-tests/test/rules.test.js
53/53 ناجح
```

هويات الاختبار الأربع كما طلبت بالضبط: مجهول، عميل أ، عميل ب، مدير (بمستند `role:'admin'` حقيقي — تماماً كما تتحقق `isAdmin()` فعلياً في الإنتاج).

**كل عملية حساسة طلبتَ اختبارها فشلت من العميل فعلياً عبر محاكي حقيقي:**
تعديل `stock`/`soldCount` مباشرة · زيادة `usedCount` · إنشاء طلب من العميل · تغيير حالة/إجمالي طلب · قراءة إشعار لعميل آخر · زيادة/إنقاص نقاط الولاء ذاتياً · إنشاء/تعديل مراجعة مباشرة · انتحال هوية في مراجعة أو طلب استرداد · استعلام غير مُصفّى على الإشعارات.

**بيئة الاختبار:** ثبّتُ Java (Eclipse Temurin 21 JRE) عبر `winget` لتشغيل محاكي Firestore الحقيقي محلياً — لم يكن متاحاً على الجهاز، وبدونه كانت هذه المرحلة ستعتمد على قراءة يدوية للقواعد فقط، وهو بالضبط ما رفضتَه صراحةً.

---

## 8) اكتشاف مهم أثناء الاختبار (شفافية كاملة)

أول صياغة كتبتها للقاعدة استخدمت `resource.data.get('target', 'all')` (بقيمة افتراضية) لتفادي خطأ تقييم على استعلام `targetId` وحدها. **الاختبارات كشفت أن هذا كان يفتح ثغرة حقيقية**: محلّل أمان استعلامات Firestore يتعامل مع القيمة الافتراضية بتساهل غير آمن، فسمح فعلياً لعميل عادي بقراءة **كل** مستندات المجموعة عبر استعلام غير مُصفّى — بما فيها إشعارات موجَّهة لعملاء آخرين، أي **نفس الثغرة التي كنا نُصلحها بالضبط**.

اكتشفتُ هذا لأن الاختبار الآلي فشل بشكل غير متوقع، لا لأنني توقعته. الإصلاح: العودة للوصول المباشر (`resource.data.target`، بلا قيمة افتراضية) وتعديل استعلام "الموجَّه لي" ليقيّد **كلا** الحقلين (`target=='user'` و`targetId==uid`) معاً بدل `targetId` وحدها — فيضمن أن كل حقل تعتمد عليه القاعدة مضمون الوجود من الاستعلام نفسه. أُعيد تشغيل كامل مجموعة الاختبارات بعدها وتأكدت من 53/53 قبل المتابعة.

هذا بالضبط سبب طلبك اختبارات حقيقية بدل مراجعة يدوية — لولاها لكان هذا الخطأ وصل للإنتاج.

---

## 9) نتيجة flutter analyze

```
5 issues found (ran in 22.3s)
```
كلها `info` تجميلية قديمة غير مرتبطة بهذه المرحلة (نفس الخمسة من قبل). **صفر أخطاء.**

أضفتُ استثناءً في `analysis_options.yaml` لمجلدي `push-server/` و`firestore-tests/` (مشروعا Node.js منفصلان داخل نفس المستودع) بعد أن التقط المحلّل ملف عيّنة `.dart` من داخل `node_modules` تابع لتبعية `firebase-tools` — لا علاقة له بتطبيق Flutter.

## 10) نتيجة flutter test

```
197/197 ناجح (كانت 183 قبل هذه المرحلة)
```
شُغِّلت **مرتين متتاليتين** كاملتين للتأكد من الاستقرار — نجحت في الاثنتين.

الإضافة من هذه المرحلة: 14 اختباراً جديداً — `notifications_feed_test.dart` (5)، `review_api_test.dart` (7)، وتوسيع اختبارات `order_submit_test.dart` (استبدل `order_submit_fallback_test.dart` المحذوف، ليعكس عدم وجود fallback بعد الآن).

## نتيجة npm test (الخادم)

```
push-server: 79/79 ناجح (كانت 61 — أضاف review-core.test.js: 18 اختباراً)
firestore-tests: 53/53 ناجح (جديد بالكامل)
```

## نتيجة Build/Deployment

- **`flutter build apk --debug`**: نجح (170 ثانية).
- **`push-server` نُشر على الإنتاج**: `alarabi-push-760jx6qc1-alarabicrystal.vercel.app` → مُعاد توجيهه لـ`alarabi-push.vercel.app`. تحقّقت مباشرة بعد النشر أن `/api/order` و`/api/review` (بما فيها `action:delete`) و`/api/notify` كلها ترفض الطلبات بلا توكن (401) — لا انحدار.

---

## 11) ⚠️ لم يُنشر بعد: firestore.rules و firestore.indexes.json

**هذا هو الفرق الوحيد بين "الكود جاهز" و"الحماية فعلية على أرض الواقع.**" الملفان معدَّلان محلياً ومُختبَران بالكامل عبر المحاكي، لكن **قواعد Production الحالية لا تزال القواعد القديمة** (نفس ما كان معمولاً به في نهاية المرحلة 10) — لم أنشرها بنفسي لأن نشر قواعد الإنتاج يتطلب تسجيل دخول Firebase CLI بحسابك (`firebase login`)، وهو إجراء مصادقة لا أقوم به عنك بنفس القيد الذي التزمت به طوال الجلسة.

**ما يلزم منك تحديداً (بنفس الطريقة التي فعلناها في مراحل سابقة):**
1. افتح Firebase Console → Firestore Database → Rules → الصق محتوى `firestore.rules` كاملاً كما هو الآن → Publish.
2. Firestore Database → Indexes → أضف الفهرسين الجديدين من `firestore.indexes.json` (`target+targetId+createdAt` و`target+createdAt` على مجموعة `notifications`) إن لم يقترحهما Firebase تلقائياً عند أول استخدام للاستعلامين الجديدين.

**حتى ذلك الحين:** التطبيق يعمل تماماً كما هو الآن (المسار الوحيد لإنشاء الطلبات والمراجعات هو الخادم فعلياً منذ المرحلة 10)، لكن الثغرات النظرية القديمة (تعديل مخزون مباشر، قراءة إشعار عميل آخر) **تبقى ممكنة تقنياً على مستوى القواعد** حتى تُنشر القواعد الجديدة — لأن لا شيء في التطبيق نفسه يحاول استغلالها، لكن قواعد Firestore القديمة لا تزال تسمح بها من خارج التطبيق (Postman/SDK مباشرة) إلى حين النشر.

---

## 12) ثغرات ومخاطر متبقية (بصراحة كاملة)

| # | الوصف | الخطورة |
|---|---|---|
| 1 | **القواعد الجديدة غير منشورة على Production بعد** (القسم 11) | **الأهم — يلغي عملياً معظم فائدة هذه المرحلة حتى تُنشر** |
| 2 | لا يوجد زر حذف مراجعة في واجهة المستخدم رغم أن الخادم يدعمه ومُختبَر | منخفضة — ميزة غير مطلوبة، فقط جاهزة لو احتجتَها |
| 3 | حقل `pricing` الجديد في مستند الطلب (منذ المرحلة 10) لا تزال شاشات تفاصيل الطلب لا تعرضه | منخفضة جداً — معلومة إضافية غير مستغَلة، لا تكسر شيئاً |
| 4 | لم يُختبَر شراء حقيقي **بعد** تشديد القواعد فعلياً على الإنتاج (لأنها غير منشورة بعد) | يُوصى باختبار طلب واحد حقيقي آخر بعد نشر القواعد، كما فعلنا في المرحلة 10 |
| 5 | التحقق من رمز الهوية في الخادم (`google-auth.js`) يعتمد على شهادات Google العامة المخبَّأة مؤقتاً — لا يوجد اختبار تكامل حقيقي لتدوير الشهادات | نظرية بحتة — Google لا تُبطل شهادات التوقيع فجأة |

**لا أدّعي أن النظام خالٍ من الثغرات.** الثقة عالية في: منطق الأسعار والمخزون والكوبونات (مُختبَر بالكامل، مرتين: وحدة واختبارات قواعد)، وفي حماية القراءة/الكتابة بعد النشر. الثقة متوسطة في: سيناريوهات تزامن عالية الحمل (لم تُختبَر تحت ضغط)، وسلوك المحاكي مقابل الإنتاج الحقيقي (الفارق الذي اكتُشف في القسم 8 يذكّر أن المحاكي نفسه قد يتصرف مختلفاً قليلاً عن الإنتاج، رغم أنه أدق أداة تحقق متاحة).

---

## توقفت هنا كما طلبت

لم أنتقل لأي مرحلة إضافية. بانتظار:
1. نشرك لـ `firestore.rules` و`firestore.indexes.json` عبر Firebase Console.
2. موافقتك (اختيارية) على اختبار طلب حقيقي واحد إضافي بعد النشر، تأكيداً نهائياً أن كل شيء يعمل مع القواعد الفعلية الجديدة معاً.
