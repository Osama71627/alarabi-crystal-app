# ملخص ما تم إنجازه — تطبيق العربية للكريستال

**آخر تحديث:** 2026-08-17

---

## 1) التصميم والواجهة

- تصميم أبيض + ذهبي كامل لواجهة المتجر (يطابق شعار الشركة)، مع إبقاء هوية لوحة الإدارة زرقاء كما هي.
- إعادة تصميم لوحة الإدارة بالكامل: القائمة السفلية، الشاشة الرئيسية، شاشات المنتجات/الطلبات/العروض/الإشعارات/المستخدمين، ونماذج الإدخال.
- إصلاح الشريط الأسود/الأصفر (مؤشر تجاوز المساحة) في شاشة تفاصيل الطلب بالإدارة.

## 2) إصلاحات أعطال حرجة

- **إصلاح تعطّل التطبيق عند الفتح**: خطأ غير معالَج في بث حالة تسجيل الدخول عند بدء التشغيل.
- **إصلاح خطأ "طلباتي"**: فهارس Firestore المركّبة كانت ناقصة (`userId` + `createdAt` وغيرها) — أُنشئت ونُشرت.
- **تعطيل مؤقت لبوابة التحقق من البريد** بناءً على الطلب — الدخول يذهب للشاشة الرئيسية مباشرة.

## 3) نظام الإشعارات (Push)

- بناء خادم خارجي مجاني منفصل (`push-server` على Vercel) لأن خطة Firebase المجانية لا تدعم Cloud Functions.
- توقيع JWT وإرسال FCM مباشرة بلا أي تبعيات خارجية (لتفادي تعليق النشر).
- إشعار فوري يصل حتى والتطبيق مغلق تماماً، وإشعار داخل التطبيق عند فتحه.
- إشعار العميل تلقائياً عند تغيّر حالة طلبه.

## 4) ميزات الطلبات والمتجر

- **تتبع الطلب اللحظي** بدون الخروج من الشاشة (بث مباشر من Firestore).
- **طباعة/حفظ الفاتورة** (PDF عربي RTL) من شاشة العميل والإدارة.
- **نظام كوبونات كامل** في لوحة الإدارة: نسبة/مبلغ ثابت/شحن مجاني، حد أدنى للطلب، تاريخ انتهاء، حد استخدام.
- **استهداف الكوبون لفئة عملاء** (الكل / عملاء جدد / كبار المشترين) مع إشعار تلقائي للفئة المستهدفة عند إنشاء الكوبون.
- **تطبيق أفضل عرض نشط تلقائياً** على السلة دون تدخل العميل.
- **فلتر مخفي بمدى سعري** (من – إلى) + فلترة حسب الفئة في شاشة المنتجات.
- استعادة **نقاط الولاء** (كانت لا تُمنح إطلاقاً) — تُمنح عند تسليم الطلب، بحماية من التكرار.
- استعادة **موافقة الاسترداد** من لوحة الإدارة (كانت معطّلة تماماً).

## 5) مراجعة أمنية شاملة (Audit)

فحص كامل للكود صنّف المشاكل حرجة/عالية/متوسطة/منخفضة، ثم أُصلحت:
- قواعد Firestore كانت منشورة فعلياً كـ "مفتوحة للجميع" — أُعيدت كتابتها بالكامل.
- حذف كود دفع إلكتروني ميت (Stripe) غير مستخدم.
- تقييد أثر أي تلاعب محتمل بالمخزون (سقف 20 لكل كتابة + تطابق مع البيع).
- تحديد سقف لكل الاستعلامات غير المحدودة (كانت تقرأ كل البيانات في كل مرة).
- توثيق ثغرة متوسطة الخطورة متبقية في قراءة الإشعارات بين المستخدمين (لم تُغلق بعد، بقرار مؤجَّل).

## 6) نقل إنشاء الطلب إلى خادم موثوق (المرحلة الأهم)

**المشكلة:** بلا خادم موثوق، كان لا بد أن تملك أي عميل مسجَّل صلاحية تعديل المخزون وعدّاد الكوبون مباشرة في Firestore — ثغرة تصعيد صلاحيات حقيقية.

**الحل المنفَّذ:**
- توسيع خادم `alarabi-push` على Vercel بنقطتي نهاية جديدتين: `/api/order` و`/api/review`.
- **الهوية تُستخرج من توقيع Firebase على رمز الهوية**، لا من أي قيمة يرسلها التطبيق.
- **الخادم هو مصدر الحقيقة الوحيد للسعر**: يقرأ المنتجات والكوبون والعروض من Firestore ويحسب كل شيء بنفسه (Subtotal, خصم الكوبون, خصم العرض, خصم النقاط, الشحن, الإجمالي). العميل يرسل فقط معرّفات المنتجات وكمياتها.
- **معاملة Firestore ذرّية واحدة**: حجز المخزون + خصم النقاط + استهلاك الكوبون + إنشاء الطلب، أو لا شيء إطلاقاً.
- **منع تكرار الطلب (Idempotency)**: معرّف الطلب يُشتق رياضياً من (صاحب الطلب + معرّف محاولة الدفع)، فإعادة إرسال نفس المحاولة (ضغطتان، أو انقطاع شبكة) لا تُنشئ طلباً ثانياً ولا تخصم مخزوناً مرتين.
- **مسار بديل مؤقت** فقط عند عطل تقني حقيقي (انقطاع شبكة/خطأ خادم) — أي رفض منطقي (مخزون ناقص، كوبون منتهٍ) يُعرض للعميل مباشرة بلا أي تجاوز.
- **77 اختباراً آلياً جديداً** (خادم + تطبيق) يغطي: تزوير السعر، تزوير الإجمالي، تزوير المخزون، انتحال هوية عميل آخر، إنشاء طلب بحالة "تم التسليم" مباشرة، تجاوز حد الكوبون، إلخ — كلها مرفوضة بنجاح.

**الاختبار النهائي — طلب حقيقي فعلي:**
تم تنفيذ طلب حقيقي واحد على الإنتاج (منتج بسعر 1125 ر.س، عرض تلقائي 10% خصم، الإجمالي 1012.5 ر.س)، وتحقّقت شخصياً من Firestore مباشرة بعده:
- ✅ طلب واحد فقط أُنشئ، بالسعر والخصم والإجمالي الصحيح تماماً.
- ✅ المخزون نقص بمقدار الكمية بالضبط (10 → 9)، والمبيعات زادت بنفس المقدار (250 → 251).
- ✅ لا كوبون استُهلك، لا نقاط مُنحت، لا بيانات قديمة تغيّرت.
- ✅ الطلب مصدره الخادم الجديد فعلياً (دليل مباشر من المستند نفسه)، وليس المسار المحلي القديم.

**النتيجة: المرحلة 10 مقبولة رسمياً وناجحة بالكامل.**

---

## الوضع الحالي — ما تبقّى وينتظر قرارك

| البند | الحالة |
|---|---|
| تشديد قواعد Firestore النهائي ومنع الكتابة المباشرة على المخزون/الكوبون | **بانتظار موافقتك** (المرحلة 11) |
| إزالة المسار المحلي القديم (بعد التشديد) | بانتظار المرحلة 11 |
| ثغرة قراءة الإشعارات بين المستخدمين (خطورة متوسطة) | موثّقة، غير مُصلَحة بعد |
| إعادة حساب تقييم المنتج تلقائياً بعد كل مراجعة | جاهزة في الخادم (`/api/review`) لكن التطبيق لم يُربط بها بعد |
| اختبار Idempotency ضد الإنتاج فعلياً | تم تخطّيه بقرارك (يعتمد على اختبارات آلية بدلاً منه) |

`flutter analyze`: **0 أخطاء** (5 ملاحظات تنسيق قديمة غير مرتبطة).
`flutter test`: **183/183 ناجح**.
اختبارات الخادم: **61/61 ناجحة**.
